window.notesData = {"ue_001":"# UE 笔记 001：碰撞与重叠系统 (Collision & Overlap)\r\n**创建时间：** 2025年12月10日\r\n**核心目标：** 掌握 Trigger（触发器）与 Blocker（阻挡物）的 C++ 实现。\r\n\r\n---\r\n\r\n## 1. 核心概念：重叠 vs 撞击\r\n\r\n在 UE 中，两个物体相遇会有两种结果，它们是两套完全独立的系统：\r\n\r\n| 类型     | 英文            | 现象                                         | 典型应用                         | 关键函数                  |\r\n| :------- | :-------------- | :------------------------------------------- | :------------------------------- | :------------------------ |\r\n| **重叠** | **Overlap**     | 像鬼魂一样**穿透**，不阻挡移动，但发送通知。 | 捡金币、进入剧情区域、陷阱触发。 | `OnComponentBeginOverlap` |\r\n| **撞击** | **Hit / Block** | 像墙壁一样**阻挡**，反弹，无法穿过。         | 子弹打墙、车撞树、物理掉落。     | `OnComponentHit`          |\r\n\r\n---\r\n\r\n## 2. 标准代码实现流程 (三步)\r\n\r\n要想让一个组件（比如球体）拥有检测功能，必须完成以下三步。\r\n\r\n### 第一步：头文件声明 (.h)\r\n**注意：** 回调函数必须加 **`UFUNCTION()`**，且参数列表必须严格遵守引擎规定。\r\n\r\n```cpp\r\n#include \"Components/SphereComponent.h\"\r\n\r\n// 声明组件\r\nUPROPERTY(VisibleAnywhere)\r\nUSphereComponent* SphereComp;\r\n\r\n// 声明重叠函数 (参数必须复制粘贴，不能错)\r\nUFUNCTION()\r\nvoid OnOverlapBegin(UPrimitiveComponent* OverlappedComp, AActor* OtherActor, UPrimitiveComponent* OtherComp, int32 OtherBodyIndex, bool bFromSweep, const FHitResult& SweepResult);\r\n\r\n// 声明撞击函数 (如果需要物理碰撞)\r\nUFUNCTION()\r\nvoid OnHit(UPrimitiveComponent* HitComp, AActor* OtherActor, UPrimitiveComponent* OtherComp, FVector NormalImpulse, const FHitResult& Hit);\r\n```\r\n\r\n### 第二步：构造函数初始化 (.cpp)\r\n**注意：** 在 `CreateDefaultSubobject` 之后，立刻进行设置和绑定。不要写在 `Tick` 里！\r\n\r\n```cpp\r\nAMyActor::AMyActor()\r\n{\r\n    // 1. 创建组件\r\n    SphereComp = CreateDefaultSubobject<USphereComponent>(TEXT(\"SphereComp\"));\r\n    RootComponent = SphereComp;\r\n    SphereComp->InitSphereRadius(100.0f);\r\n\r\n    // 2. 设置碰撞套餐 (Profile) 和 开关\r\n    // --- 方案 A: 做触发器 (重叠) ---\r\n    SphereComp->SetCollisionProfileName(TEXT(\"Trigger\")); // 预设：QueryOnly + Overlap\r\n    SphereComp->SetGenerateOverlapEvents(true);           // 【总开关】必须开！如果不导致不发通知\r\n\r\n    // --- 方案 B: 做物理障碍 (撞击) ---\r\n    // SphereComp->SetCollisionProfileName(TEXT(\"BlockAll\")); // 预设：QueryOnly + Block\r\n    // SphereComp->SetNotifyRigidBodyCollision(true);         // 【总开关】对应蓝图的 Simulation Generates Hit Events\r\n\r\n    // 3. 绑定回调函数 (接电话线)\r\n    SphereComp->OnComponentBeginOverlap.AddDynamic(this, &AMyActor::OnOverlapBegin);\r\n    // SphereComp->OnComponentHit.AddDynamic(this, &AMyActor::OnHit);\r\n}\r\n```\r\n\r\n### 第三步：实现逻辑 (.cpp)\r\n**注意：** 养成检查指针有效性的习惯。\r\n\r\n```cpp\r\nvoid AMyActor::OnOverlapBegin(UPrimitiveComponent* OverlappedComp, AActor* OtherActor, UPrimitiveComponent* OtherComp, int32 OtherBodyIndex, bool bFromSweep, const FHitResult& SweepResult)\r\n{\r\n    // 1. 检查 OtherActor 是否存在\r\n    // 2. 检查 OtherActor 是不是自己 (重叠可能会触发到自己的组件)\r\n    if (OtherActor && (OtherActor != this)) \r\n    {\r\n        UE_LOG(LogTemp, Warning, TEXT(\"检测到重叠： %s\"), *OtherActor->GetName());\r\n        \r\n        // 可以在这里写扣血、加分逻辑\r\n        // OtherActor->TakeDamage(...)\r\n    }\r\n}\r\n```\r\n\r\n---\r\n\r\n## 3. 底层机制：Query vs Physics\r\n\r\n不要被名字骗了，\"Block\" 不等于 \"Physics\"。\r\n\r\n| 设置名                | 含义       | 行为                                                         | 例子                 |\r\n| :-------------------- | :--------- | :----------------------------------------------------------- | :------------------- |\r\n| **Query Only**        | **仅查询** | **不模拟物理**（无重力），但可以通过扫掠（Sweep）检测到。如果设为 Block，**能挡住人**。 | 墙壁、地板、触发器   |\r\n| **Physics Only**      | **仅物理** | **模拟物理**（有重力、反弹），但不响应射线检测或移动查询。   | 纯装饰性的碎石       |\r\n| **Collision Enabled** | **双修**   | 既能挡住人，又能被物理引擎计算（最耗性能）。                 | 足球、可被踢飞的箱子 |\r\n\r\n**关键结论：**\r\n*   做**不动的墙**：用 `Query Only` + `Block`。\r\n*   做**鬼魂触发器**：用 `Query Only` + `Overlap`。\r\n\r\n---\r\n\r\n## 4. 委托 (Delegate) 与 回调 (Callback)\r\n\r\n**定义：**\r\n一种“通知机制”。引擎底层不知道你写了什么函数，但它留了一个“插口”。你把你的函数地址（电话号码）插进去，出事了引擎就会调用你的函数。\r\n\r\n**绑定时机：**\r\n1.  **构造函数 (最佳 )**：对于自带的组件 (`CreateDefaultSubobject`)，出生即绑定，最稳妥。\r\n2.  **BeginPlay (可行)**：对于运行时动态生成的组件，或者引用别人的组件。\r\n3.  **Tick (绝对禁止 )**：每帧绑定会造成极大的性能浪费和逻辑冗余。\r\n\r\n---\r\n\r\n## 5. 排坑指南 (Debug Checklist)\r\n\r\n如果你写了代码，进游戏撞上去**没有任何反应**，请按顺序检查：\r\n\r\n1.  **宏忘了加：** `.h` 里的函数没加 `UFUNCTION()`？ -> 绑定会静默失败。\r\n2.  **开关没开：**\r\n    *   重叠：没写 `SetGenerateOverlapEvents(true)`？\r\n    *   撞击：没写 `SetNotifyRigidBodyCollision(true)`？\r\n3.  **预设不对：**\r\n    *   你是 `Trigger`，对方是 `Ignore Trigger`？(拍巴掌需要两只手)。\r\n    *   你是 `Block`，对方也是 `Block`，才能触发 `Hit`。\r\n    *   你是 `Overlap`，或者对方是 `Overlap`，只能触发 `Overlap`，不能触发 `Hit`。\r\n4.  **改了名字/热重载 bug：**\r\n    *   如果之前改过组件名，蓝图可能坏了。**关闭编辑器 -> IDE 编译 -> 重启编辑器**。\r\n\r\n---\n","ue_002":"# UE 笔记 002：通信机制 —— 委托与事件 (Delegates & Events)\r\n**创建时间：** 2025年12月10日\r\n**前置知识：** UE 001 碰撞系统 (碰撞的底层就是委托)\r\n**核心目标：** 掌握 C++ 与 蓝图、对象 与 对象 之间的 **解耦通信**。\r\n\r\n---\r\n\r\n## 1. 核心概念：委托 (Delegate) 与 广播 (Broadcast)\r\n\r\n不要去死记硬背，用 **“博主与粉丝”** 的关系来理解：\r\n\r\n| 概念                 | 代码对应               | 现实比喻            | 作用                                     |\r\n| :------------------- | :--------------------- | :------------------ | :--------------------------------------- |\r\n| **委托 (Delegate)**  | `FOnSomethingHappened` | **关注列表** (List) | 一个对象，专门用来记“谁关注了我”。       |\r\n| **绑定 (Bind)**      | `.AddDynamic(...)`     | **点关注**          | 别的对象把自己的函数地址存进列表里。     |\r\n| **广播 (Broadcast)** | `.Broadcast(...)`      | **发推送**          | 博主喊话，列表里所有人的函数都会被执行。 |\r\n\r\n*   **本质：** **观察者模式 (Observer Pattern)**。\r\n*   **价值：** **解耦**。博主（如玩家角色）不需要引用粉丝（如UI、音效管理器、成就系统），只管喊话，谁爱听谁听。\r\n\r\n---\r\n\r\n## 2. 核心主力：动态多播委托 (Dynamic Multicast)\r\n\r\n这是 **90%** 的使用场景。只有它能**同时支持 C++ 和 蓝图**，且能**群发**。\r\n\r\n### A. 标准实现四步走 (背诵)\r\n\r\n#### 1. 声明类型 (头文件顶部)\r\n**位置：** `#include` 下面，`UCLASS` 上面。\r\n**宏解析：** `DECLARE` (声明) + `DYNAMIC` (蓝图可见) + `MULTICAST` (群发) + `DELEGATE` (委托) + `_OneParam` (参数数量)。\r\n\r\n```cpp\r\n// 声明一个叫 FOnHurt 的委托类型，带一个 float 参数 (参数名叫 Damage)\r\nDECLARE_DYNAMIC_MULTICAST_DELEGATE_OneParam(FOnHurt, float, Damage);\r\n```\r\n\r\n#### 2. 定义变量 (类内部)\r\n**位置：** `public` 区域。\r\n**必须加：** `BlueprintAssignable` (让蓝图能在侧边栏分配事件)。\r\n\r\n```cpp\r\nUPROPERTY(BlueprintAssignable, Category = \"Events\")\r\nFOnHurt OnHurt; // 这就是那个“喇叭”对象\r\n```\r\n\r\n#### 3. 执行广播 (Cpp 逻辑中)\r\n**位置：** 事件发生的源头 (比如 `TakeDamage` 函数)。\r\n\r\n```cpp\r\n// 喊话！告诉所有关注者：我扣血了，数值是 CurrentHP\r\nOnHurt.Broadcast(CurrentHP);\r\n```\r\n\r\n#### 4. 接收绑定 (别人)\r\n*   **蓝图侧：** 选中变量 -> Details 面板 -> 点击绿色的 `+` 号 -> 自动生成事件节点。\r\n*   **C++ 侧：**\r\n    ```cpp\r\n    // 必须用 AddDynamic (因为是动态委托)\r\n    // 这里的函数 MyUIUpdate 必须加 UFUNCTION()\r\n    Player->OnHurt.AddDynamic(this, &AHUD::MyUIUpdate);\r\n    ```\r\n\r\n---\r\n\r\n## 3. 进阶分类：委托的 2x2 矩阵\r\n\r\nUE 提供了四种基础委托，根据 **“能不能给蓝图用”** 和 **“是一对一还是一对多”** 来划分。\r\n\r\n|                      | **单播 (Single)**          | **多播 (Multicast)**                            |\r\n| :------------------- | :------------------------- | :---------------------------------------------- |\r\n| **静态 (C++ Only)**  | `DECLARE_DELEGATE`         | `DECLARE_MULTICAST_DELEGATE`                    |\r\n| **动态 (Blueprint)** | `DECLARE_DYNAMIC_DELEGATE` | **`DECLARE_DYNAMIC_MULTICAST_DELEGATE` (主力)** |\r\n\r\n### A. 静态与动态的区别\r\n*   **动态 (Dynamic)：** 慢，依赖反射，**蓝图可用**，函数必须加 `UFUNCTION()`。\r\n*   **静态 (Static)：** 快，纯 C++ 函数指针，**蓝图不可见**，支持复杂参数（如非 UObject 指针）。\r\n\r\n### B. 单播与多播的区别\r\n*   **多播 (Multicast)：** 大喇叭。**无返回值**（没法听100个人的回答）。使用 `Broadcast()`。\r\n*   **单播 (Single)：** 打电话。**支持返回值 (`RetVal`)**。使用 `Execute()`。\r\n\r\n---\r\n\r\n## 4. 特殊通信：C++ 调用蓝图 (BlueprintImplementableEvent)\r\n\r\n这是 **C++ 定义接口，蓝图负责实现** 的专用机制。通常用于 **“表现层”**（特效、声音、UI动画）。\r\n\r\n### 写法：\r\n```cpp\r\n// .h 文件\r\n// 这里的 Category 是必须的\r\nUFUNCTION(BlueprintImplementableEvent, Category = \"Effects\")\r\nvoid OnPlayAttackEffect(); \r\n```\r\n\r\n### 规则：\r\n1.  **C++ 不要写函数体！** (`.cpp` 里不要写实现)，UE 会自动生成。\r\n2.  **蓝图实现：** 在蓝图里右键搜 `Event On Play Attack Effect`，会出现红色事件节点。\r\n3.  **调用：** C++ 里直接写 `OnPlayAttackEffect();` 即可触发。\r\n\r\n*   **变种：** `BlueprintNativeEvent`（C++ 有默认实现，蓝图可选重写）。C++ 实现函数名为 `函数名_Implementation`。\r\n\r\n---\r\n\r\n## 5. 带返回值的委托 (RetVal)\r\n\r\n**场景：** 商店系统问背包系统：“你有多少钱？”。必须用 **单播**。\r\n\r\n### 写法：\r\n**坑点：** 宏的第一个参数是**返回值类型**！\r\n\r\n```cpp\r\n// 1. 声明：返回 int32\r\nDECLARE_DELEGATE_RetVal(int32, FOnCheckMoney);\r\n\r\n// 2. 定义\r\nFOnCheckMoney CheckMoneyDelegate;\r\n\r\n// 3. 调用 (必须判空！)\r\nif (CheckMoneyDelegate.IsBound()) \r\n{\r\n    int32 Money = CheckMoneyDelegate.Execute(); // 接住返回值\r\n}\r\n```\r\n\r\n---\r\n\r\n## 6. 避坑指南 (Checklist)\r\n\r\n1.  **绑定失败？**\r\n    *   检查目标函数有没有加 **`UFUNCTION()`**（动态委托强制要求）。\r\n    *   检查**参数类型**是否完全一致（包括 const/引用）。\r\n2.  **崩溃 (Crash)？**\r\n    *   单播委托（Execute）前没有检查 **`IsBound()`**。\r\n    *   多播委托（Broadcast）即使没人绑也是安全的，不会崩。\r\n3.  **蓝图搜不到？**\r\n    *   委托变量忘了加 `UPROPERTY(BlueprintAssignable)`。\r\n    *   函数忘了加 `UFUNCTION(BlueprintCallable)`。\r\n\r\n\r\n\r\n| 委托类型        | 调用方法        | 核心绑定方法 (常用) | 备注                      |\r\n| --------------- | --------------- | ------------------- | ------------------------- |\r\n| **1. 普通单播** | **Execute()**   | BindUObject()       | 必须绑定，否则 Crash      |\r\n| **2. 普通多播** | **Broadcast()** | AddUObject()        | 没人听也没事，不崩        |\r\n| **3. 动态单播** | **Execute()**   | BindDynamic()       | 蓝图可用，必须绑定        |\r\n| **4. 动态多播** | **Broadcast()** | AddDynamic()        | **UI 用的就是它**，最常用 |\n","ue_003":"# UE 笔记 003：组件生命周期与高级通信\r\n**创建时间：** 2025年12月10日\r\n**核心目标：** 掌握对象创建原理，精通委托宏与字符串处理，避开编辑器热重载坑。\r\n\r\n---\r\n\r\n## 1. 组件创建与生命周期 (`CreateDefaultSubobject`)\r\n**核心原则：** 构造函数造器官，运行时造物品。\r\n\r\n### A. 语法拆解\r\n```cpp\r\n// 只能在构造函数使用！\r\n// 模板参数 <T>：指定类型\r\n// TEXT(\"Name\")：指定唯一身份证 (用于序列化保存数据)\r\nSphereComp = CreateDefaultSubobject<USphereComponent>(TEXT(\"SphereComp\"));\r\n```\r\n\r\n### B. `CreateDefaultSubobject` vs `NewObject`\r\n| 函数                         | 使用场所                    | 用途                           | 蓝图表现                              |\r\n| :--------------------------- | :-------------------------- | :----------------------------- | :------------------------------------ |\r\n| **`CreateDefaultSubobject`** | **构造函数**                | **器官** (天生自带的组件)      | 永久显示在组件列表，**无法删除**。    |\r\n| **`NewObject`**              | **运行时** (BeginPlay/Tick) | **物品** (后天生成的子弹/特效) | 动态存在，列表里通常看不见，GC 管理。 |\r\n\r\n### C. 可见性陷阱 (Visible vs Edit)\r\n对于 **原生组件 (`CreateDefaultSubobject`)**：\r\n*   **`VisibleAnywhere`**：**推荐**。只读引用，但可修改内部属性（材质/位置）。\r\n*   **`EditAnywhere`**：**无效**。引擎为了防止崩溃，强制锁死“删除/替换”功能，表现得和 Visible 一样。\r\n\r\n---\r\n\r\n## 2. UE 命名规范体系 (Prefixes)\r\n**核心原则：** 看首字母识身份，写错导致 UHT 报错。\r\n\r\n| 前缀    | 全称                  | 含义               | 内存管理       | 例子                   |\r\n| :------ | :-------------------- | :----------------- | :------------- | :--------------------- |\r\n| **`A`** | **Actor**             | 场景物体           | GC (Spawn)     | `AMyCharacter`         |\r\n| **`U`** | **UObject**           | 引擎对象/组件/资源 | GC (NewObject) | `UStaticMeshComponent` |\r\n| **`F`** | **Struct/Foundation** | 原生 C++ 类/结构体 | 手动/栈        | `FVector`, `FString`   |\r\n| **`T`** | **Template**          | 模板容器           | 视内容而定     | `TArray`, `TMap`       |\r\n| **`E`** | **Enum**              | 枚举               | -              | `ECollisionChannel`    |\r\n| **`I`** | **Interface**         | 接口               | -              | `IInteractable`        |\r\n\r\n---\r\n\r\n## 3. 字符串三巨头 (String Types)\r\n**核心原则：** 不要混用，该给谁看就用哪种。\r\n\r\n| 类型          | 用途         | 转换特性                          | 场景                                           |\r\n| :------------ | :----------- | :-------------------------------- | :--------------------------------------------- |\r\n| **`FString`** | **逻辑处理** | 可变，操作最丰富 (Printf, +, Len) | 拼路径、打印 Log。                             |\r\n| **`FName`**   | **系统索引** | 不可变，极快 (Hash)               | 找骨骼、找组件、找资源 Key。                   |\r\n| **`FText`**   | **UI 显示**  | 本地化支持 (多语言)               | **给玩家看的文字**。禁止用 `TEXT()` 直接赋值。 |\r\n\r\n*   **`TEXT(\"...\")` 宏**：将字符串转为 TCHAR 数组，用于适配多平台编码。\r\n\r\n---\r\n\r\n## 4. 碰撞设置进阶 (Collision Advanced)\r\n**核心原则：** 学名与小名的对应关系。\r\n\r\n### A. 命名混淆对照表\r\n| 功能               | C++ 函数名 (学名)                   | 编辑器显示名 (小名)                 |\r\n| :----------------- | :---------------------------------- | :---------------------------------- |\r\n| **撞击 (Hit)**     | `SetNotifyRigidBodyCollision(true)` | **Simulation Generates Hit Events** |\r\n| **重叠 (Overlap)** | `SetGenerateOverlapEvents(true)`    | **Generate Overlap Events**         |\r\n\r\n### B. 阻挡原理\r\n*   **Query Only + Block**：不模拟物理（无重力），但是 **实心** 的，能挡住射线和角色移动（如墙壁）。\r\n*   **Physics Only**：模拟物理，但不响应查询（如纯视觉碎块）。\r\n\r\n---\r\n\r\n## 5. 委托宏与通信进阶 (Delegate Macros)\r\n**核心原则：** 宏名决定功能，参数决定数据。\r\n\r\n### A. 宏的填空题\r\n`DECLARE_[DYNAMIC]_[MULTICAST]_DELEGATE_[RetVal]_[OneParam](Name, Type, Var...)`\r\n\r\n1.  **`DYNAMIC`**：蓝图要用就加，不用不加。\r\n2.  **`MULTICAST`**：要群发（广播）就加，要返回值（一对一）就不加。\r\n3.  **`RetVal`**：**带返回值**（必须是单播）。注意：**返回值类型写在宏的第一个参数**。\r\n\r\n### B. C++ 与 蓝图混合 (Hybrid)\r\n*   **`BlueprintImplementableEvent`**：\r\n    *   **C++**：只声明，**不写函数体**。\r\n    *   **蓝图**：实现逻辑。\r\n    *   **用途**：C++ 通知蓝图播放声光电特效。\r\n*   **`BlueprintNativeEvent`**：\r\n    *   **C++**：写 `函数名_Implementation` 作为保底逻辑。\r\n    *   **蓝图**：可重写覆盖。\r\n\r\n---\r\n\r\n## 6. 避坑指南 (Critical Bugs)\r\n\r\n1.  **组件改名/类型修改 Bug**：\r\n    *   **现象**：蓝图细节面板变白/属性丢失。\r\n    *   **原因**：热重载 (Hot Reload) 无法处理内存布局变更。\r\n    *   **解法**：**关闭编辑器 -> IDE Rebuild -> 重启编辑器**。\r\n2.  **参数命名雷区**：\r\n    *   声明委托时，参数名**不要**使用 `TEXT`、`check`、`None` 等宏或关键字。\r\n3.  **Include 规则**：\r\n    *   `.h` 中尽量用 `class UCameraComponent;` (前置声明)。\r\n    *   `.cpp` 中必须 `#include \"Components/CameraComponent.h\"` (包含头文件)。\n","ue_004":"# UE 笔记 004：逻辑优化与高级交互\r\n**创建时间：** 2025年12月11日\r\n**核心目标：** 掌握 Tick 优化、软硬引用区别、以及严格的事件权限控制。\r\n\r\n---\r\n\r\n## 1. Tick 频率控制与时间管理\r\n**核心原则：** 不要把所有逻辑都塞进每一帧。\r\n\r\n### A. 降低频率的三种流派\r\n| 方法               | 原理                                            | 适用场景                                    | 性能                               |\r\n| :----------------- | :---------------------------------------------- | :------------------------------------------ | :--------------------------------- |\r\n| **累加 DeltaTime** | 在 Tick 里用 `if (Timer > Interval)` 卡住逻辑。 | **移动中**顺便做检测 (如每秒扫描一次敌人)。 | 中                                 |\r\n| **Tick Interval**  | `PrimaryActorTick.TickInterval = 2.0f;`         | **全员减速**。Actor 移动也会变卡。          | 远景物体、不需要平滑移动的刷怪塔。 |\r\n| **定时器 (Timer)** | `GetWorld()->GetTimerManager().SetTimer(...)`   | **完全脱离 Tick**。独立运行。               | 回血、Buff、倒计时。**架构最优**。 |\r\n\r\n---\r\n\r\n## 2. Overlap Stay (持续重叠) 的实现\r\n**核心原则：** UE 没有 `OnOverlapStay`，必须手动维护名单。\r\n\r\n### 标准实现 (登记法)\r\n1.  **容器**：使用 **`TSet<AActor*>`** (自动去重，查询快)。\r\n2.  **Begin**：`Set.Add(OtherActor)` (进门登记)。\r\n3.  **End**：`Set.Remove(OtherActor)` (出门注销)。\r\n4.  **Tick**：`for (AActor* Victim : Set)` (遍历名单，持续伤害)。\r\n    *   **关键点**：遍历时必须检查 **`IsValid(Victim)`**，防止对象中途被销毁导致崩溃。\r\n\r\n---\r\n\r\n## 3. UE5 指针体系 (Pointers)\r\n**核心原则：** 头文件用新式，源文件用老式，资源用软引用。\r\n\r\n### A. 指针三巨头\r\n| 类型                    | 全名               | 用途              | 什么时候用                                               |\r\n| :---------------------- | :----------------- | :---------------- | :------------------------------------------------------- |\r\n| **`T*`**                | **Raw Pointer**    | **原生指针**      | **.cpp 逻辑里**、函数参数、局部变量。最快。              |\r\n| **`TObjectPtr<T>`**     | **Object Pointer** | **懒加载指针**    | **.h 成员变量** (`UPROPERTY`)。优化编辑器加载速度。      |\r\n| **`TSoftObjectPtr<T>`** | **Soft Pointer**   | **软引用 (路径)** | **资产引用** (UI图片/模型)。**按需加载**，防止内存爆炸。 |\r\n\r\n### B. 动态加载 (Soft Reference)\r\n*   **同步加载**：`SoftRef.LoadSynchronous()` (会卡顿，适合小资源)。\r\n*   **异步加载**：配合 `StreamableManager` (无缝，适合大资源)。\r\n\r\n---\r\n\r\n## 4. 获取对象引用的四种方式\r\n**核心原则：** 拿到指针才能调用函数。\r\n\r\n1.  **编辑期指定**：`UPROPERTY(EditAnywhere)` -> 吸管吸取。 (**最常用**)\r\n2.  **物理接触**：`OnOverlap` -> `Cast<AMyClass>(OtherActor)`。\r\n3.  **全图搜索**：`UGameplayStatics::GetActorOfClass`。 (**性能差，慎用**)\r\n4.  **自己生成**：`SpawnActor` 的返回值。\r\n\r\n---\r\n\r\n## 5. C++ 与 蓝图混合 (Hybrid)\r\n**核心原则：** C++ 定义接口，蓝图实现表现。\r\n\r\n### A. `BlueprintImplementableEvent`\r\n*   **特点**：C++ 只声明 (不写函数体)，蓝图写逻辑 (Event节点)。\r\n*   **用途**：C++ 通知蓝图播放 **特效、音效、Timeline 动画**。\r\n\r\n### B. `BlueprintNativeEvent`\r\n*   **特点**：C++ 写保底逻辑 (`_Implementation`)，蓝图可重写覆盖。\r\n*   **用途**：交互系统 (Interact)，默认是捡起，蓝图可以改成爆炸。\r\n\r\n---\r\n\r\n## 6. 事件 (Event) vs 委托 (Delegate)\r\n**核心原则：** Event 是加了“权限锁”的委托。\r\n\r\n| 特性         | 动态多播委托                | 事件 (Event)                           |\r\n| :----------- | :-------------------------- | :------------------------------------- |\r\n| **宏定义**   | `DECLARE_DYNAMIC...`        | `DECLARE_EVENT_NumParams(老大类, ...)` |\r\n| **绑定方式** | `.AddDynamic`               | `.AddUObject` (纯C++)                  |\r\n| **广播权限** | **公开** (谁都能 Broadcast) | **私有** (只有**老大类**能 Broadcast)  |\r\n| **蓝图支持** |  支持                      |  不支持                               |\r\n| **适用场景** | **Gameplay / UI**           | **底层系统 / 核心架构**                |\r\n\r\n---\r\n\r\n## 7. 概念纠错 (Terminology)\r\n*   **Override (重写)**：子类覆盖父类虚函数 (同名同参)。\r\n*   **Overload (重载)**：同名不同参 (如 `Add(int)` 和 `Add(float)`)。\r\n*   **Proxy (代理)**：UE 中指 **网络同步代理**，不要把 Delegate 叫成代理。\n","ue_005":"# UE 笔记 005：角色控制与增强输入 \r\n**创建时间：** 2025年12月12日\r\n**核心目标：** 彻底搞懂 UE5 输入系统，实现标准 TPS 角色移动。\r\n\r\n---\r\n\r\n## 1. 继承体系选择 (Class Hierarchy)\r\n\r\n**核心原则：** 不要重复造轮子。\r\n\r\n| 父类             | 核心特征                      | 移动方式               | 适用场景            |\r\n| :--------------- | :---------------------------- | :--------------------- | :------------------ |\r\n| **`AActor`**     | 死物。无控制器附身能力。      | `AddForce` (物理推力)  | 子弹、墙壁、掉落物  |\r\n| **`APawn`**      | 载具。可被 `Possess` (附身)。 | 手写推力/阻力          | 飞机、坦克、RTS相机 |\r\n| **`ACharacter`** | **人形。自带移动组件。**      | **`AddMovementInput`** | **主角**、怪兽、NPC |\r\n\r\n*   **为什么选 Character？** 它自带的 `UCharacterMovementComponent` 完美处理了 **上下坡、台阶、游泳、网络同步**。自己写 Pawn 的移动逻辑极其痛苦。\r\n\r\n---\r\n\r\n## 2. 增强输入系统 (Enhanced Input) —— **深度拆解**\r\n\r\n不要把它想成代码，把它想成 **“接水管”** 的过程。\r\n\r\n### 第一层：资产配置 (在编辑器里做)\r\n\r\n你需要创建三种资产，它们相当于 **水管的阀门和滤网**。\r\n\r\n1.  **Input Action (IA)** —— **\"水龙头\"**\r\n    *   **定义**：定义信号的数据类型。\r\n    *   **设置**：\r\n        *   `IA_Move`: Value Type 选 **Axis2D (Vector2D)** —— 输出 (X, Y) 两个数。\r\n        *   `IA_Jump`: Value Type 选 **Digital (bool)** —— 输出 按了/没按。\r\n    \r\n2.  **Input Mapping Context (IMC)** —— **\"配管图\"**\r\n    *   **定义**：决定按哪个键出水，以及水怎么流。\r\n    *   **设置 (关键！)**：\r\n        *   **W (前进)**: 添加 **Swizzle Input Axis Values** (把按键产生的 X=1 挪到 Y 轴，输出 `0, 1`)。\r\n        *   **S (后退)**: 添加 **Swizzle** + **Negate** (取反，输出 `0, -1`)。\r\n        *   **A (左移)**: 添加 **Negate** (输出 `-1, 0`)。\r\n        *   **D (右移)**: 啥都不加 (默认输出 `1, 0`)。\r\n\r\n### 第二层：C++ 绑定 (代码四步走)\r\n\r\n#### 第 1 步：引入模块 (Build.cs)\r\n*   **动作**：打开 `MyGame.Build.cs`。\r\n*   **代码**：`PublicDependencyModuleNames.AddRange(new string[] { \"Core\", ..., \"EnhancedInput\" });`\r\n*   **原因**：增强输入是一个插件模块，不引进来，编译器就不认识相关类。\r\n\r\n#### 第 2 步：头文件声明 (.h)\r\n*   **动作**：准备好变量来接编辑器里的资产。\r\n\r\n```cpp\r\n#include \"InputActionValue.h\" // 必须包含！否则 FInputActionValue 报错\r\n\r\nclass UInputMappingContext; // 前置声明\r\nclass UInputAction;\r\n\r\nUCLASS()\r\nclass AMyCharacter : public ACharacter\r\n{\r\n    // ...\r\npublic:\r\n    // 让策划填资产的坑位\r\n    UPROPERTY(EditAnywhere, Category=\"Input\")\r\n    UInputMappingContext* DefaultMappingContext;\r\n\r\n    UPROPERTY(EditAnywhere, Category=\"Input\")\r\n    UInputAction* MoveAction;\r\n\r\n    UPROPERTY(EditAnywhere, Category=\"Input\")\r\n    UInputAction* JumpAction;\r\n\r\nprotected:\r\n    // 逻辑函数：注意参数必须是 const FInputActionValue&\r\n    void Move(const FInputActionValue& Value);\r\n    \r\n    // 重写系统函数\r\n    virtual void SetupPlayerInputComponent(class UInputComponent* PlayerInputComponent) override;\r\n    virtual void BeginPlay() override;\r\n};\r\n```\r\n\r\n#### 第 3 步：开启系统 & 绑定函数 (.cpp)\r\n\r\n```cpp\r\n#include \"EnhancedInputComponent.h\"\r\n#include \"EnhancedInputSubsystems.h\"\r\n\r\n// 【开启系统】在 BeginPlay\r\nvoid AMyCharacter::BeginPlay()\r\n{\r\n    Super::BeginPlay();\r\n\r\n    // 1. 拿到 Controller\r\n    if (APlayerController* PC = Cast<APlayerController>(Controller))\r\n    {\r\n        // 2. 拿到 增强输入子系统 (Subsystem)\r\n        if (UEnhancedInputLocalPlayerSubsystem* Subsystem = ULocalPlayer::GetSubsystem<UEnhancedInputLocalPlayerSubsystem>(PC->GetLocalPlayer()))\r\n        {\r\n            // 3. 激活映射表 (IMC)\r\n            // 没这一行，按键毫无反应！\r\n            Subsystem->AddMappingContext(DefaultMappingContext, 0);\r\n        }\r\n    }\r\n}\r\n\r\n// 【绑定函数】在 SetupPlayerInputComponent\r\nvoid AMyCharacter::SetupPlayerInputComponent(UInputComponent* PlayerInputComponent)\r\n{\r\n    // 1. 强转为增强版组件\r\n    if (UEnhancedInputComponent* EI = Cast<UEnhancedInputComponent>(PlayerInputComponent))\r\n    {\r\n        // 2. 绑定 Move\r\n        // ETriggerEvent::Triggered 意味着只要按住，每帧都调用 Move 函数\r\n        EI->BindAction(MoveAction, ETriggerEvent::Triggered, this, &AMyCharacter::Move);\r\n\r\n        // 3. 绑定 Jump (直接绑 Character 自带的 Jump 函数)\r\n        EI->BindAction(JumpAction, ETriggerEvent::Started, this, &ACharacter::Jump);\r\n        EI->BindAction(JumpAction, ETriggerEvent::Completed, this, &ACharacter::StopJumping);\r\n    }\r\n}\r\n```\r\n\r\n#### 第 4 步：实现移动逻辑 (Move) —— **难点解析**\r\n\r\n这里最容易晕的是 **坐标系转换**。\r\n\r\n```cpp\r\nvoid AMyCharacter::Move(const FInputActionValue& Value)\r\n{\r\n    // 1. 拿到输入的二维数据 (X:左右, Y:前后)\r\n    // 这个数据是我们在 IMC 里配置好传过来的\r\n    FVector2D MovementVector = Value.Get<FVector2D>();\r\n\r\n    if (Controller != nullptr)\r\n    {\r\n        // 2. 找出“哪里是前？”\r\n        // 我们不能直接用 Actor 的 Forward，因为我们要往“摄像机看的前方”走\r\n        const FRotator Rot = Controller->GetControlRotation(); // 拿到摄像机的角度\r\n        const FRotator YawRot(0, Rot.Yaw, 0); // 【关键】只取水平旋转，忽略低头抬头\r\n\r\n        // 3. 算出方向向量\r\n        // UE坐标系：X=前，Y=右，Z=上\r\n        const FVector ForwardDir = FRotationMatrix(YawRot).GetUnitAxis(EAxis::X); // 算出哪个方向是前\r\n        //FVector forwardDirection = FRotator (YawRot).RotateVector(FVector::ForwardVector);也可以\r\n        const FVector RightDir   = FRotationMatrix(YawRot).GetUnitAxis(EAxis::Y); // 算出哪个方向是右\r\n\r\n        // 4. 下指令\r\n        // 用输入的 Y (前后) 驱动世界的 ForwardDir\r\n        AddMovementInput(ForwardDir, MovementVector.Y);\r\n        // 用输入的 X (左右) 驱动世界的 RightDir\r\n        AddMovementInput(RightDir, MovementVector.X);\r\n    }\r\n}\r\n```\r\n\r\n---\r\n\r\n## 3. 摄像机与旋转配置 (TPS 黄金法则)\r\n**核心原则：** 身体、杆子、相机，各听各的。\r\n\r\n### A. 组件层级\r\n*   `Capsule` (Root)\r\n    \r\n    *   `Mesh`\r\n    *   `SpringArm` (弹簧臂)\r\n        *   `Camera` (相机)\r\n    \r\n*   **代码写法**：\r\n    ```cpp\r\n    SpringArm->SetupAttachment(GetCapsuleComponent()); // 挂在胶囊体上\r\n    Camera->SetupAttachment(SpringArm);                // 挂在杆子上\r\n    ```\r\n    *注意：访问胶囊体必须用 `GetCapsuleComponent()`，还要 `#include \"Components/CapsuleComponent.h\"`。*\r\n    \r\n    **初始视角问题**：`SpringArm` 的 `SetRelativeRotation` 会被 Controller 覆盖。必须在 `BeginPlay` 里修改 `Controller->SetControlRotation`。\r\n\r\n### B. 旋转开关配置 (背诵)\r\n要实现 **“人往哪跑脸朝哪，鼠标转动只转相机”** 的效果：\r\n\r\n1.  **身体 (Character)**：\r\n    *   `bUseControllerRotationYaw = false;` (身体别跟着鼠标转)\r\n    *   `GetCharacterMovement()->bOrientRotationToMovement = true;` (身体跟着速度方向转)\r\n2.  **杆子 (SpringArm)**：\r\n    *   `bUsePawnControlRotation = true;` (杆子听鼠标的，带着相机转)\r\n3.  **相机 (Camera)**：\r\n    *   `bUsePawnControlRotation = false;` (相机别动，死死粘在杆子上)\r\n\r\n---\r\n\r\n## 4. 常见报错速查\r\n\r\n1.  **报错 \"Incomplete type\" (不完整类型)**\r\n    *   **原因**：你用了组件的函数或变量，但只写了前置声明。\r\n    *   **解法**：在 `.cpp` 顶部 `#include` 那个组件的头文件。\r\n\r\n2.  **细节面板里组件属性不见了**\r\n    *   **原因**：改了 C++ 组件名字或类型，热重载 Bug。\r\n    *   **解法**：关掉编辑器 -> Rider 编译 (Ctrl+Shift+B) -> 重启编辑器。\r\n\r\n3.  **组件无法删除/替换**\r\n    *   **原因**：原生组件 (`CreateDefaultSubobject`) 被引擎保护。\r\n    *   **解法**：正常。原生组件应该用 `VisibleAnywhere`，不要试图在蓝图里删它。\r\n\r\n---\r\n\r\n## 4. 跳跃与手感调优 (Physics Feel)\r\n\r\n**原则：** 动作游戏手感 = 高重力 + 高初速度。\r\n\r\n### A. 基础跳跃\r\n\r\n*   **起跳**：绑定 `&ACharacter::Jump`。\r\n*   **截断**：绑定 `&ACharacter::StopJumping` (松手即停)。\r\n\r\n### B. 手感参数 (CharacterMovement)\r\n\r\n*   **`GravityScale`**：重力倍率。建议 **1.5 ~ 2.0** (拒绝太空漫步)。\r\n*   **`JumpZVelocity`**：起跳速度。配合重力调大，建议 **600 ~ 700**。\r\n*   **`AirControl`**：空中控制力。建议 **0.5**。\r\n*   **`RotationRate`**：转身速度。建议 **540 ~ 720** (拒绝迟钝)。\r\n\r\n### C. 变量归属 (找对人)\r\n\r\n*   `JumpMaxHoldTime` 属于 **Character** (直接写)。\r\n*   `GravityScale` 属于 **CharacterMovement** (需要 `GetCharacterMovement()`)。\r\n\r\n---\r\n\r\n## 5. C++ 工程规范\r\n\r\n**原则：** 规范写法防崩溃。\r\n\r\n1.  **组件指针**：\r\n    *   头文件：`class USpringArmComponent* Arm;` (前置声明)。\r\n    *   源文件：`#include \"GameFramework/SpringArmComponent.h\"` (包含头文件)。\r\n    *   创建：`CreateDefaultSubobject` (仅构造函数)。\r\n    *   可见性：**`VisibleAnywhere`** (原生组件锁死删除)。\r\n2.  **访问父类私有组件**：\r\n    *   不能直接写 `CapsuleComponent`。\r\n    *   必须用 `GetCapsuleComponent()`。\r\n3.  **报错处理**：\r\n    *   \"Incomplete type\" -> **缺头文件**。\r\n    *   \"细节面板空白\" -> **热重载 Bug** (重启编辑器)。\r\n\r\n---\r\n\r\n## 6. 美术资产导入 (Art Pipeline)\r\n\r\n**核心原则：** 导入设置决定模型质量。\r\n\r\n*   **模型变软/平滑**：`Normal Import Method` 改为 **`Import Normals`** (保留硬边)。\r\n*   **零件散落**：\r\n    *   **道具**：勾选 `Combine Meshes` (合并)。\r\n    *   **角色**：勾选 `Skeletal Mesh` (骨骼网格体)。\r\n    *   **可动机械 (车)**：**不勾选**，导入蓝图组装。\r\n*   **模型死黑**：\r\n    *   检查光照 (SkyLight/Lumen)。\r\n    *   检查模型 (距离场分辨率设为0 / 顶点色设为 Ignore)。\r\n\r\n---\r\n\r\n## 7. C++ 语法补充\r\n*   **分号 `;`**：类定义 (`class {};`) 必须加分号，逻辑块 (`if {}`) 不加。\r\n*   **`class` 前置声明**：`.h` 成员变量推荐写法 `UPROPERTY() class UType* Var;`，为了编译速度。\n","ue_006":"# UE 笔记 006：控制器架构与输入系统 \r\n**创建时间：** 2025年12月13日\r\n**核心目标：** 实现“大脑控制身体”的架构，掌握 Controller 内的增强输入实现与指针安全。\r\n\r\n---\r\n\r\n## 1. 架构升级：大脑与身体 (Controller vs Character)\r\n**核心原则：** **Input (输入)** 写在大脑里，**Action (行动)** 写在身体里。\r\n\r\n| 类                      | 身份            | 职责                                        | 关键差异                                |\r\n| :---------------------- | :-------------- | :------------------------------------------ | :-------------------------------------- |\r\n| **`APlayerController`** | **大脑 / 逻辑所有者** | 接收按键、处理 UI、切换控制权 (上车/下车)。 | **存活久**。角色死了它还在 (可以重生)。 |\r\n| **`ACharacter`**        | **表现对象 / 傀儡** | 执行具体的动作 (跑、跳)、播放动画。         | **易销毁**。死了就 Destroy 了。         |\r\n\r\n### A. 写法差异对照表\r\n从 Character 搬家到 Controller，必须注意这三个变化：\r\n\r\n| 步骤           | 在 Character 里写                   | 在 PlayerController 里写                |\r\n| :------------- | :---------------------------------- | :-------------------------------------- |\r\n| **绑定函数名** | `SetupPlayerInputComponent`         | **`SetupInputComponent`** (少个 Player) |\r\n| **获取子系统** | `GetController()->GetLocalPlayer()` | **`GetLocalPlayer()`** (直接拿自己)     |\r\n| **输入组件**   | 函数参数传进来的                    | 类的成员变量 **`InputComponent`**       |\r\n| **控制对象**   | `this`                              | **`GetPawn()`** 或 **`GetCharacter()`** |\r\n\r\n---\r\n\r\n## 2. 增强输入实战：Controller 篇 (标准代码)\r\n\r\n这是最核心的代码模版，请严格遵守函数名和调用方式。\r\n\r\n### A. 头文件声明 (.h)\r\n```cpp\r\n#pragma once\r\n#include \"CoreMinimal.h\"\r\n#include \"GameFramework/PlayerController.h\"\r\n#include \"InputActionValue.h\" // 必须包含\r\n#include \"MyController.generated.h\"\r\n\r\nclass UInputMappingContext;\r\nclass UInputAction;\r\n\r\nUCLASS()\r\nclass MYGAME_API AMyController : public APlayerController\r\n{\r\n    GENERATED_BODY()\r\n\r\npublic:\r\n    // --- 1. 资产槽位 (暴露给蓝图) ---\r\n    UPROPERTY(EditAnywhere, Category=\"Input\")\r\n    TObjectPtr<UInputMappingContext> DefaultMappingContext;\r\n\r\n    UPROPERTY(EditAnywhere, Category=\"Input\")\r\n    TObjectPtr<UInputAction> MoveAction;\r\n\r\n    UPROPERTY(EditAnywhere, Category=\"Input\")\r\n    TObjectPtr<UInputAction> JumpAction;\r\n\r\nprotected:\r\n    // --- 2. 重写关键函数 (注意函数名) ---\r\n    virtual void BeginPlay() override;\r\n    virtual void SetupInputComponent() override;\r\n\r\n    // --- 3. 逻辑函数 ---\r\n    void Move(const FInputActionValue& Value);\r\n    void JumpPressed();\r\n    void JumpReleased();\r\n};\r\n```\r\n\r\n### B. 源文件实现 (.cpp)\r\n```cpp\r\n#include \"MyController.h\"\r\n#include \"EnhancedInputComponent.h\"\r\n#include \"EnhancedInputSubsystems.h\"\r\n#include \"GameFramework/Character.h\" // 必须引用 Character\r\n\r\n// --- 1. 启用映射表 (IMC) ---\r\nvoid AMyController::BeginPlay()\r\n{\r\n    Super::BeginPlay();\r\n\r\n    // Controller 直接拥有 LocalPlayer，获取子系统最方便\r\n    if (UEnhancedInputLocalPlayerSubsystem* Subsystem = ULocalPlayer::GetSubsystem<UEnhancedInputLocalPlayerSubsystem>(GetLocalPlayer()))\r\n    {\r\n        if (DefaultMappingContext)\r\n        {\r\n            Subsystem->AddMappingContext(DefaultMappingContext, 0);\r\n        }\r\n    }\r\n}\r\n\r\n// --- 2. 绑定输入 (接线) ---\r\nvoid AMyController::SetupInputComponent()\r\n{\r\n    Super::SetupInputComponent();\r\n\r\n    // 强转成员变量 InputComponent\r\n    if (UEnhancedInputComponent* EI = Cast<UEnhancedInputComponent>(InputComponent))\r\n    {\r\n        // 绑定移动\r\n        if (MoveAction)\r\n        {\r\n            EI->BindAction(MoveAction, ETriggerEvent::Triggered, this, &AMyController::Move);\r\n        }\r\n        // 绑定跳跃 (注意：不能直接绑 Character::Jump，要绑自己的中转函数)\r\n        if (JumpAction)\r\n        {\r\n            EI->BindAction(JumpAction, ETriggerEvent::Started, this, &AMyController::JumpPressed);\r\n            EI->BindAction(JumpAction, ETriggerEvent::Completed, this, &AMyController::JumpReleased);\r\n        }\r\n    }\r\n}\r\n\r\n// --- 3. 移动逻辑 (找替身干活) ---\r\nvoid AMyController::Move(const FInputActionValue& Value)\r\n{\r\n    FVector2D MovementVector = Value.Get<FVector2D>();\r\n\r\n    // 获取当前控制的身体 (Pawn)\r\n    if (APawn* ControlledPawn = GetPawn())\r\n    {\r\n        // 算出方向 (基于 Controller 的朝向)\r\n        const FRotator Rotation = GetControlRotation();\r\n        const FRotator YawRotation(0, Rotation.Yaw, 0);\r\n        const FVector ForwardDir = FRotationMatrix(YawRotation).GetUnitAxis(EAxis::X);\r\n        const FVector RightDir = FRotationMatrix(YawRotation).GetUnitAxis(EAxis::Y);\r\n        \r\n        // 命令身体移动\r\n        ControlledPawn->AddMovementInput(ForwardDir, MovementVector.Y);\r\n        ControlledPawn->AddMovementInput(RightDir, MovementVector.X);\r\n    }\r\n}\r\n\r\n// --- 4. 跳跃中转 (Character 特有功能) ---\r\nvoid AMyController::JumpPressed()\r\n{\r\n    // 使用 GetCharacter() 快捷函数 (自带 Cast)\r\n    if (ACharacter* MyChar = GetCharacter())\r\n    {\r\n        MyChar->Jump();\r\n    }\r\n}\r\n\r\nvoid AMyController::JumpReleased()\r\n{\r\n    if (ACharacter* MyChar = GetCharacter())\r\n    {\r\n        MyChar->StopJumping();\r\n    }\r\n}\r\n```\r\n\r\n---\r\n\r\n## 3. 指针安全判空 (Pointer Safety)\r\n**核心原则：** 永远假设指针可能是空的。\r\n\r\n| 写法               | 安全等级 | 适用场景            | 备注                                                    |\r\n| :----------------- | :------- | :------------------ | :------------------------------------------------------ |\r\n| **`IsValid(Obj)`** | ⭐⭐⭐⭐⭐    | **UObject / Actor** | **首选**。不仅查空，还查是否“即将被销毁(PendingKill)”。 |\r\n| **`if (Obj)`**     | ⭐⭐⭐⭐     | **UObject / Actor** | **次选**。UE 重载了 bool 转换，等同于 IsValid。         |\r\n| **`!= nullptr`**   | ⭐⭐       | **原生 C++ 指针**   | **慎用**。只查地址是否为0，不管对象死活。               |\r\n\r\n---\r\n\r\n## 4. 跳跃与手感调优 (Physics Feel)\r\n**核心原则：** 动作游戏手感 = **高重力** + **高初速度**。\r\n\r\n### A. 关键参数设置\r\n*   **`GravityScale`** (重力倍率)：设为 **1.5 ~ 2.0** (拒绝太空漫步)。\r\n*   **`JumpZVelocity`** (起跳速度)：设为 **600 ~ 700** (配合重力加高)。\r\n*   **`AirControl`** (空中控制)：设为 **0.5** (空中可微调)。\r\n*   **`RotationRate`** (转身速度)：设为 **540 ~ 720** (拒绝迟钝)。\r\n\r\n### B. 变量归属 (找对人)\r\n*   `JumpMaxHoldTime` (蓄力时间) -> 属于 **Character** (直接写)。\r\n*   `GravityScale` / `JumpZVelocity` -> 属于 **CharacterMovement** 组件。\r\n\r\n---\r\n\r\n## 5. 常见报错速查 (Troubleshooting)\r\n\r\n1.  **按键没反应？**\r\n    *   **检查 GameMode**：`Player Controller Class` 必须换成你写的 `BP_MyController`。\r\n    *   **检查 IMC**：`BP_MyController` 里的 `DefaultMappingContext` 必须赋值。\r\n2.  **无法解析符号 `JumpMaxHoldTime`**：\r\n    *   错因：去组件里找了。\r\n    *   解法：`this->JumpMaxHoldTime`。\r\n3.  **类型不完整 (Incomplete Type)**：\r\n    *   错因：缺头文件。\r\n    *   解法：`#include \"GameFramework/CharacterMovementComponent.h\"`。\n","ue_007":"# UE 笔记 007：战斗系统与伤害框架 (Combat & Damage)\r\n**创建时间：** 2025年12月15日\r\n**核心目标：** 掌握 Actor 的动态生成、父子级挂接、以及投射物运动组件的使用，实现武器装备、弹道物理、标准伤害流程及特效反馈。\r\n\r\n---\r\n\r\n## 1. 核心机制：模板与实例 (Class vs Instance)\r\n**原则：** 用“模具”来生成“蛋糕”。\r\n\r\n### A. `TSubclassOf<T>` (模具)\r\n这是 UE C++ 中最常用的 **类型限制器**。\r\n*   **写法**：`UPROPERTY() TSubclassOf<AMyWeapon> WeaponClass;`\r\n*   **作用**：\r\n    1.  **类型安全**：在编辑器里，策划只能把 `BP_Rifle`（枪的子类）拖进去，不能把 `BP_Enemy`（敌人）拖进去。\r\n    2.  **生成依据**：它是 `SpawnActor` 函数必须的参数，告诉引擎要造什么东西。\r\n*   **对比**：\r\n    *   `UClass*`：万能模具（不安全，啥都能存）。\r\n    *   `AMyWeapon*`：这是造好的**真枪**（实例指针）。\r\n\r\n---\r\n\r\n## 2. 生成系统：`SpawnActor` (Creation)\r\n**原则：** 运行时动态创建 Actor 的唯一正规途径。\r\n\r\n### A. 代码公式\r\n```cpp\r\n// 1. 准备生成参数 (出生证明)\r\nFActorSpawnParameters SpawnParams;\r\nSpawnParams.Owner = this;      // 归属权 (网络同步用)\r\nSpawnParams.Instigator = this; // 肇事者 (逻辑判定用)\r\n\r\n// 2. 执行生成\r\nAMyWeapon* NewWeapon = GetWorld()->SpawnActor<AMyWeapon>(\r\n    WeaponClass, // 模具\r\n    SpawnLoc,    // 位置\r\n    SpawnRot,    // 旋转\r\n    SpawnParams  // 参数\r\n);\r\n```\r\n\r\n### B. 关键参数详解 (`Instigator`)\r\n*   **在 Character 里写时**：`Instigator = this;` (我是源头)。\r\n*   **在 Weapon 里写时**：`Instigator = GetInstigator();` (传递接力棒，拿我的人是源头)。\r\n*   **作用**：建立 **责任链**。子弹打中人后，系统能顺藤摸瓜找到是谁开的枪。\r\n\r\n---\r\n\r\n## 3. 挂接系统：`AttachToComponent` (Hierarchy)\r\n**原则：** 建立父子级关系，实现“拿在手里”。\r\n\r\n### A. 骨骼插槽 (Socket)\r\n*   **美术工作**：在骨骼树 (`hand_r`) 上添加 Socket (`WeaponSocket`)，调整好位置。\r\n*   **代码获取**：\r\n    *   `GetMesh()->GetSocketLocation(FName(\"WeaponSocket\"))`\r\n\r\n### B. 粘贴规则 (`FAttachmentTransformRules`)\r\n当把独立的枪粘在手上时，需要指定规则：\r\n*   **`SnapToTarget`**：**吸附**。枪的位置/旋转完全变成插槽的位置/旋转。\r\n*   **`KeepWorld`**：**保持**。保持枪在世界中的绝对位置（拿起枪时枪会飞过来）。\r\n*   **`SnapToTargetNotIncludingScale`** (推荐)：位置旋转吸附，但**缩放保持枪自己的大小**（防止枪被手的骨骼缩放压扁）。\r\n\r\n---\r\n\r\n## 4. 投射物物理：`ProjectileMovementComponent`\r\n**原则：** 不要自己写 `Tick` 移动，用专用引擎。\r\n\r\n这是 UE 专门为子弹、火箭、手雷设计的物理组件。\r\n\r\n### A. 核心参数配置 (在构造函数中)\r\n```cpp\r\nProjectileComp = CreateDefaultSubobject<UProjectileMovementComponent>(TEXT(\"PComp\"));\r\n\r\n// 1. 谁是物理实体？(通常是根组件 CollisionComp)\r\nProjectileComp->UpdatedComponent = CollisionComp;\r\n\r\n// 2. 速度控制\r\nProjectileComp->InitialSpeed = 3000.f; // 初速度\r\nProjectileComp->MaxSpeed = 3000.f;     // 最大速度\r\n\r\n// 3. 行为控制\r\nProjectileComp->bRotationFollowsVelocity = true; // 像箭一样，头永远朝向飞行的方向\r\nProjectileComp->bShouldBounce = true;            // 开启反弹 (跳弹)\r\nProjectileComp->Bounciness = 0.6f;               // 弹性 (0=死面团, 1=永动球)\r\nProjectileComp->ProjectileGravityScale = 0.0f;   // 重力 (0=直线飞, 1=抛物线)\r\n```\r\n\r\n---\r\n\r\n## 5. 架构优化：单一职责 (Refactoring)\r\n**原则：** 角色只管扣扳机，武器负责发射。\r\n\r\n*   **初级写法**：在 `Character::Fire()` 里写 `SpawnActor<Bullet>`。\r\n*   **进阶写法**：\r\n    1.  **Character**：调用 `CurrentWeapon->Fire()`。\r\n    2.  **Weapon**：实现 `Fire()`，在里面写 `SpawnActor`。\r\n    3.  **Weapon**：暴露 `GetWeaponMesh()` 接口，以便获取枪口插槽 (`Muzzle`) 位置。\r\n\r\n---\r\n\r\n## 6. 伤害框架 (Damage Framework)\r\n**核心原则：** 使用引擎内置的“矛与盾”协议，实现通用伤害。\r\n\r\n### A. 矛：`ApplyDamage` (发起者)\r\n通常写在子弹的 `OnHit` 里。\r\n```cpp\r\nUGameplayStatics::ApplyDamage(\r\n    OtherActor,                 // 受害者\r\n    DamageAmount,               // 伤害值\r\n    GetInstigatorController(),  // 凶手的大脑 (用于计分)\r\n    this,                       // 凶器 (子弹)\r\n    UDamageType::StaticClass()  // 类型\r\n);\r\n```\r\n\r\n### B. 盾：`TakeDamage` (承受者)\r\n在 `AActor` 子类中重写。\r\n```cpp\r\nfloat TakeDamage(...) \r\n{\r\n    float ActualDamage = Super::TakeDamage(...); // 必须调父类\r\n    // 扣血逻辑\r\n    Health -= ActualDamage;\r\n    if (Health <= 0) Destroy();\r\n    return ActualDamage; // 返回实际扣除量\r\n}\r\n```\r\n\r\n### C. 责任链 (Instigator Chain)\r\n**问题**：如何确保子弹打死人后，经验值加给玩家？\r\n*   **传递链**：`Character` (this) -> `Weapon` (SetInstigator) -> `Projectile` (SetInstigator) -> `ApplyDamage`。\r\n*   **断链后果**：打死怪了，但系统认为是“自然死亡”，没人得分。\r\n\r\n---\r\n\r\n## 7. 视觉反馈 (VFX & SFX)\r\n**核心原则：** 射后不理 (Fire and Forget)。\r\n\r\n### A. 播放方式\r\n*   **错误**：在对象身上挂特效组件。对象 `Destroy`，特效也跟着消失（截断）。\r\n*   **正确**：`SpawnEmitterAtLocation` / `SpawnSystemAtLocation` (Niagara)。生成一个独立的特效 Actor，播完自动销毁。\r\n\r\n### B. Niagara 接入\r\n1.  **Build.cs**：加入 `\"Niagara\"` 模块。\r\n2.  **头文件**：`TObjectPtr<UNiagaraSystem>`。\r\n3.  **源文件**：`UNiagaraFunctionLibrary::SpawnSystemAtLocation(...)`。\r\n\r\n---\r\n\r\n## 8. C++ 语法与规范进阶\r\n*   **`FORCEINLINE`**：强制内联，用于 Getter 函数，极大优化高频调用性能。\r\n*   **`const` 函数**：`GetX() const`，承诺不修改类状态。\r\n*   **`static` 函数**：如 `UGameplayStatics`，需要传入 `this` (WorldContext) 才能找到世界。\r\n\r\n---\n","ue_008":"# UE 笔记 008：射线检测与空间数学 (Trace & Math)\r\n**创建时间：** 2025年12月18日\r\n**核心能力：** 主动探测游戏世界、解决 3D 空间几何问题、优化操作手感。\r\n\r\n---\r\n\r\n## 1. 射线检测 (Line Trace / Raycast)\r\n**定义：** 从点 A 到点 B 发射一条无限细的隐形线，返回第一个阻挡它的物体信息。\r\n\r\n### A. 核心代码模版 (直接背诵/复制)\r\n```cpp\r\n// 1. 准备输入参数\r\nFHitResult OutHit; // 输出结果（一张白纸，引擎填空）\r\nFVector Start = CameraLoc;\r\nFVector End = Start + (Dir * Distance);\r\n\r\n// 2. 配置查询参数 (重要！)\r\nFCollisionQueryParams Params;\r\nParams.AddIgnoredActor(this); // 忽略自己 (防止射中自己的胶囊体/枪)\r\n// Params.AddIgnoredActor(CurrentWeapon); // 也可以忽略手里的枪\r\nParams.bTraceComplex = true; // true=检测模型多边形(精准但慢)，false=检测简模碰撞体(快)\r\n\r\n// 3. 执行检测 (按通道)\r\n// ECC_Visibility: 可见性通道 (最常用)\r\n// ECC_Camera: 相机通道 (防穿墙用)\r\nbool bHit = GetWorld()->LineTraceSingleByChannel(\r\n    OutHit, \r\n    Start, \r\n    End, \r\n    ECC_Visibility, \r\n    Params\r\n);\r\n\r\n// 4. 调试画线 (仅在 Debug/Development 模式生效)\r\n// #include \"DrawDebugHelpers.h\"\r\n// DrawDebugLine(GetWorld(), Start, End, FColor::Red, false, 2.0f);\r\n```\r\n\r\n### B. `FHitResult` 结构体详解 (情报单)\r\n当 `bHit == true` 时，`OutHit` 里会有数据：\r\n\r\n| 变量名             | 类型          | 含义           | 用途                                          |\r\n| :----------------- | :------------ | :------------- | :-------------------------------------------- |\r\n| **`GetActor()`**   | `AActor*`     | **撞到了谁**   | 核心判断：`if (Cast<AEnemy>(Hit.GetActor()))` |\r\n| **`Location`**     | `FVector`     | **撞击点**     | 生成特效的位置、计算弹道终点。                |\r\n| **`ImpactNormal`** | `FVector`     | **撞击面法线** | 面的朝向。用于让弹孔贴图“贴合”墙面。          |\r\n| **`BoneName`**     | `FName`       | **骨骼名**     | FPS 爆头判定 (`head`)。                       |\r\n| **`Component`**    | `UPrimitive*` | **撞到的组件** | 判断是撞到了车轮还是车身。                    |\r\n\r\n---\r\n\r\n## 2. 形状扫描 (Sweep / Shape Trace)\r\n**定义：** 射线是一根针，Sweep 是一根**粗管子**。\r\n**适用场景：** 对精度要求不高的交互（如捡东西）、近战挥砍检测。\r\n\r\n### A. 代码差异\r\n与 LineTrace 只有两处不同：\r\n1.  **函数名**：`SweepSingleByChannel`。\r\n2.  **多一个参数**：`FCollisionShape`。\r\n\r\n```cpp\r\n// 创建一个半径 30cm 的球体形状\r\nFCollisionShape MyColSphere = FCollisionShape::MakeSphere(30.0f);\r\n\r\n// 执行扫描\r\nGetWorld()->SweepSingleByChannel(OutHit, Start, End, FQuat::Identity, ECC_Visibility, MyColSphere, Params);\r\n```\r\n\r\n---\r\n\r\n## 3. 数学插值 (Interpolation)\r\n**核心原则：** 游戏里除了传送，所有变化都应该是**渐变**的。\r\n\r\n### A. 三种插值函数的区别 (必考)\r\n\r\n| 函数                   | 原理                                     | 曲线特征                                        | 适用场景                                                    |\r\n| :--------------------- | :--------------------------------------- | :---------------------------------------------- | :---------------------------------------------------------- |\r\n| **`FMath::Lerp`**      | **按比例**<br>`A + (B-A)*Alpha`          | **线性** (除非 Alpha 自带曲线)                  | Timeline 动画、UI 颜色渐变、知道明确的开始/结束时间。       |\r\n| **`FMath::InterpTo`**  | **平滑追赶**<br>`VInterpTo`, `RInterpTo` | **减速曲线 (Ease-Out)**<br>越近越慢，无限逼近。 | **Tick 中使用**。摄像机跟随、准星回正、物体吸附、平滑转身。 |\r\n| **`InterpConstantTo`** | **匀速追赶**                             | **匀速直线**<br>速度恒定。                      | 电梯、推拉门、追踪导弹。                                    |\r\n\r\n### B. `InterpTo` 的使用陷阱\r\n*   **不要用 `==` 判断到达**：因为是无限逼近，浮点数永远不会相等。\r\n*   **正确判达方式**：\r\n    ```cpp\r\n    // 距离平方 < 阈值平方 (比如 1cm)\r\n    if (FVector::DistSquared(Current, Target) < 1.0f) \r\n    {\r\n        SetActorLocation(Target); // 强制吸附，消除误差\r\n        // 结束逻辑...\r\n    }\r\n    ```\r\n\r\n---\r\n\r\n## 4. TPS 射击视差修正 (Aim Offset Solution)\r\n**问题：** 枪在右边，准星在中间。直接按枪口方向射击，永远打不中准星瞄准的点。\r\n\r\n### A. 修正算法 (三步)\r\n1.  **眼见为实**：从 **摄像机** 向 **屏幕中心** 发射射线。\r\n    *   `TargetPoint = Hit.bBlockingHit ? Hit.Location : Hit.TraceEnd;`\r\n2.  **距离限制 (优化)**：\r\n    *   如果目标太远（>50米），强行把 `TargetPoint` 拉回到相机前方 50 米处。\r\n    *   *目的：防止射击近处敌人时，因为瞄准了极远处的背景，导致子弹横穿屏幕，出现“躺枪”现象。*\r\n3.  **指哪打哪**：计算新方向。\r\n    *   `NewRotation = (TargetPoint - MuzzleLocation).Rotation();`\r\n\r\n---\r\n\r\n## 5. 工程化封装思维 (Engineering)\r\n**核心原则：** 不要把底层 API 散落在业务逻辑里。\r\n\r\n### A. 封装工具类 (`UBlueprintFunctionLibrary`)\r\n创建一个静态函数库（如 `MyGameplayUtils`），把 `LineTrace` 的 10 行代码封装成 1 行。\r\n\r\n```cpp\r\n// 静态函数必须传入 WorldContextObject 才能获取 GetWorld()\r\nstatic bool PerformTrace(const UObject* WorldContextObject, FHitResult& OutHit, ...);\r\n```\r\n\r\n### B. `UFUNCTION` 的使用准则\r\n*   **什么时候加？**\r\n    *   给蓝图用 (`BlueprintCallable`)。\r\n    *   绑定动态委托 (`OnHit`, `OnOverlap`)。\r\n    *   网络同步 (`Server`, `Client`)。\r\n*   **什么时候不加？**\r\n    *   纯 C++ 内部调用的 Helper 函数（如上面的 `PerformTrace`，如果不想暴露给蓝图就不加）。这样编译更快，开销更小。\r\n\r\n---\r\n\r\n## 6. C++ 语法细节备忘\r\n*   **`const FHitResult&`**：输入参数（只读引用）。\r\n*   **`FHitResult&`**：输出参数（引用）。**在 C++ 里，想让函数返回多个值，就用这个。**\r\n*   **`FVector::DistSquared`**：比 `Dist` 快，因为不用开根号。判定距离优先用这个。\r\n\r\n---\n","ue_009":"# UE 笔记 009：通用交互与调试系统 (Interaction & Debugging)\r\n**创建时间：** 2025年12月19日\r\n**核心目标：** 掌握接口系统实现通用交互，熟练使用多种输入调试手段。\r\n\r\n---\r\n\r\n## 1. 接口系统 (Interfaces) —— 解耦核心\r\n**核心原则：** 不要问“你是谁”（Cast），只问“你能做什么”（Interface）。\r\n\r\n### A. 为什么用接口？\r\n*   **Cast 地狱 (Bad)**：`if (Cast<Door>) Open(); else if (Cast<Chest>) Open(); ...`\r\n*   **接口通用 (Good)**：`if (Implements<Interact>) Execute_Interact();`\r\n*   **价值**：无论未来加什么新物体（NPC、开关、载具），只要继承了接口，角色代码一行都不用改。\r\n\r\n### B. 语法三段式 (必背)\r\n接口是 UE C++ 中语法最怪异的部分，分三个阶段：\r\n\r\n| 阶段               | 关键字/函数                         | 细节说明                                                     |\r\n| :----------------- | :---------------------------------- | :----------------------------------------------------------- |\r\n| **1. 定义 (.h)**   | **`UINTERFACE`** & **`class I...`** | `U`类给反射看，`I`类写纯虚函数。函数需加 `BlueprintNativeEvent` 以支持蓝图。 |\r\n| **2. 实现 (.cpp)** | **`_Implementation`**               | 子类重写时，函数名后面必须加 `_Implementation`。             |\r\n| **3. 调用 (Call)** | **`Execute_函数名`**                | **静态函数调用！** 不能用 `->`。必须把受害者作为第一个参数传进去。 |\r\n\r\n### C. 代码模版\r\n**定义接口：**\r\n```cpp\r\n// .h\r\nUINTERFACE(MinimalAPI)\r\nclass UInteractInterface : public UInterface { GENERATED_BODY() };\r\n\r\nclass MYGAME_API IInteractInterface\r\n{\r\n    GENERATED_BODY()\r\npublic:\r\n    // 声明：允许蓝图重写，也允许C++实现\r\n    UFUNCTION(BlueprintNativeEvent, BlueprintCallable)\r\n    void Interact(APawn* InstigatorPawn);\r\n};\r\n```\r\n\r\n**实现接口 (物体)：**\r\n```cpp\r\n// AChest.h\r\nclass AChest : public AActor, public IInteractInterface { ... };\r\n\r\n// AChest.cpp (重写逻辑)\r\nvoid AChest::Interact_Implementation(APawn* InstigatorPawn)\r\n{\r\n    OpenChest(); // 开箱逻辑\r\n}\r\n```\r\n\r\n**调用接口 (角色)：**\r\n```cpp\r\n// AHero.cpp\r\nif (HitActor && HitActor->Implements<UInteractInterface>()) // 这里的泛型填 U类\r\n{\r\n    // 静态调用：I类::Execute_名字(目标, 参数...)\r\n    IInteractInterface::Execute_Interact(HitActor, this);\r\n}\r\n```\r\n\r\n---\r\n\r\n## 2. 交互逻辑闭环 (Interaction Loop)\r\n**核心原则：** 射线检测 + 接口调用 = 万能交互键。\r\n\r\n### 流程图：\r\n1.  **Input (按 E)**：触发 `IA_Interact`。\r\n2.  **Trace (射线)**：调用 `DoLineTrace`，获取 `FHitResult`。\r\n3.  **Check (判断)**：`Hit.GetActor()` 是否实现了接口？\r\n4.  **Execute (执行)**：调用 `Execute_Interact`。\r\n5.  **Feedback (反馈)**：物体播放动画、销毁或生成物品。\r\n\r\n---\r\n\r\n## 3. 程序员调试输入 (Developer Inputs)\r\n**核心原则：** 绕过繁琐的资产配置，用代码快速测试逻辑。\r\n\r\n### 三种流派对比\r\n\r\n| 流派           | 写法                     | 优点                             | 缺点                 | 适用场景                     |\r\n| :------------- | :----------------------- | :------------------------------- | :------------------- | :--------------------------- |\r\n| **控制台指令** | **`UFUNCTION(Exec)`**    | **最专业**。不占键位，支持传参。 | 需要打字。           | 修改无敌模式、刷怪、改属性。 |\r\n| **硬编码绑定** | **`BindKey(EKeys::F1)`** | **最快**。一键触发。             | 绑定函数不能带参数。 | 临时测试某个函数是否生效。   |\r\n| **Tick 检测**  | `IsInputKeyDown`         | 简单粗暴。                       | 容易脏，每帧触发。   | 测试“按住不放”的逻辑。       |\r\n\r\n### 代码实战\r\n**1. Exec (推荐):**\r\n```cpp\r\n// .h\r\nUFUNCTION(Exec)\r\nvoid DebugGodMode();\r\n\r\n// 游戏运行按 ~ 键，输入 DebugGodMode 回车即可触发\r\n```\r\n\r\n**2. BindKey (临时):**\r\n```cpp\r\n// .cpp SetupInputComponent\r\n// 注意：DebugFunc 必须是无参函数 void Func()\r\nInputComponent->BindKey(EKeys::F1, IE_Pressed, this, &AMyClass::DebugFunc);\r\n```\r\n*   **常见报错**：`BindAction` 传了 `EKeys::E` -> 类型不匹配。`BindAction` 要的是名字(FName)，`BindKey` 要的才是键位(FKey)。\r\n\r\n---\r\n\r\n## 4. 工程化思维进阶 (Engineering)\r\n**核心原则：** 封装通用逻辑，减少重复代码。\r\n\r\n*   **蓝图函数库 (`UBlueprintFunctionLibrary`)**：\r\n    *   把 `DoLineTrace` 这种通用的工具函数，封装成 `static` 函数，放在库里。\r\n    *   **好处**：主角能用，敌人能用，炮台也能用，不用到处复制粘贴。\r\n*   **包含原则 (Include)**：\r\n    *   **`Cast<T>`** 需要 `#include \"T.h\"` (要知道家谱)。\r\n    *   **接口调用** 需要 `#include \"Interface.h\"`。\r\n    *   **`GetCharacter()`** 比 `Cast<ACharacter>(GetPawn())` 更优雅。\r\n\r\n---\r\n\r\n### 🏁 阶段总结\r\n\r\n现在已经攻克了 **UE C++ 交互开发** 的三大难关：\r\n1.  **射线检测** (眼睛)。\r\n2.  **接口系统** (万能钥匙)。\r\n3.  **调试手段** (后门)。\n","ue_010":"# UE 笔记 010：属性组件与 UI 系统\r\n**创建时间：** 2025年12月22日\r\n**核心目标：** 将数值逻辑剥离为组件，通过委托驱动 UI 更新，实现 MVVM 架构。\r\n\r\n---\r\n\r\n## 1. 核心基石：属性组件 (`UAttributeComponent`)\r\n\r\n我们要把“血量”变成一个**独立的配件**，而不是写死在角色身体里。\r\n\r\n### A. 头文件 (.h) —— 定义协议\r\n这里有两个关键点：**委托宏的声明** 和 **变量的权限控制**。\r\n\r\n```cpp\r\n#pragma once\r\n#include \"CoreMinimal.h\"\r\n#include \"Components/ActorComponent.h\"\r\n#include \"MyAttributeComponent.generated.h\"\r\n\r\n// 【关键点 1】声明一个多播委托 (大喇叭)\r\n// 为什么是 Dynamic？因为 UI (蓝图) 也需要监听它。\r\n// 为什么是 FourParams？因为 UI 更新需要知道：当前血、最大血 (算百分比)，甚至想知道是谁打的。\r\nDECLARE_DYNAMIC_MULTICAST_DELEGATE_FourParams(FOnHealthChanged, float, NewHealth, float, MaxHealth, AActor*, InstigatorActor, float, Delta);\r\n\r\nUCLASS( ClassGroup=(Custom), meta=(BlueprintSpawnableComponent) )\r\nclass MYGAME_API UMyAttributeComponent : public UActorComponent\r\n{\r\n    GENERATED_BODY()\r\n\r\npublic:\t\r\n    UMyAttributeComponent();\r\n\r\n    // 【关键点 2】定义委托变量\r\n    // BlueprintAssignable：让蓝图能在侧边栏分配事件，或者拖出来 Bind。\r\n    UPROPERTY(BlueprintAssignable)\r\n    FOnHealthChanged OnHealthChanged;\r\n\r\n    // 【关键点 3】唯一的修改入口\r\n    // 所有的加血、扣血都必须走这个函数，不要直接修改 Health 变量。\r\n    // 返回值 bool：表示是否成功修改了 (活着才算成功)。\r\n    UFUNCTION(BlueprintCallable, Category = \"Attributes\")\r\n    bool ApplyHealthChange(float Delta, AActor* InstigatorActor);\r\n\r\n    // Getter：给 C++ 逻辑用的快速访问接口\r\n    float GetHealth() const { return Health; }\r\n    float GetMaxHealth() const { return MaxHealth; }\r\n\r\nprotected:\r\n    // 【关键点 4】数据保护\r\n    // 设为 protected，防止外部类 (如 Character) 直接 Hero->Health = 0; 从而绕过委托广播。\r\n    UPROPERTY(EditDefaultsOnly, BlueprintReadOnly, Category = \"Attributes\")\r\n    float Health;\r\n\r\n    UPROPERTY(EditDefaultsOnly, BlueprintReadOnly, Category = \"Attributes\")\r\n    float MaxHealth = 100.0f;\r\n};\r\n```\r\n\r\n### B. 源文件 (.cpp) —— 实现逻辑\r\n这里是 **数据驱动** 的核心。\r\n\r\n```cpp\r\nbool UMyAttributeComponent::ApplyHealthChange(float Delta, AActor* InstigatorActor)\r\n{\r\n    // 1. 逻辑保护 (可选)：如果已经死了，就不再扣血，或者无敌状态不扣血\r\n    // if (Health <= 0 && Delta < 0) return false;\r\n\r\n    // 2. 数值修改\r\n    float OldHealth = Health;\r\n    // Clamp 是必须的，防止血量变成 -10 或者 150\r\n    Health = FMath::Clamp(Health + Delta, 0.0f, MaxHealth);\r\n\r\n    // 3. 计算实际变化量\r\n    float ActualDelta = Health - OldHealth;\r\n\r\n    // 4. 【核心】广播通知\r\n    // 只有数值真的变了才喊话。省性能，防 Bug。\r\n    if (ActualDelta != 0.0f)\r\n    {\r\n        // 这里的参数顺序必须和 DECLARE 宏里的一模一样！\r\n        OnHealthChanged.Broadcast(Health, MaxHealth, InstigatorActor, ActualDelta);\r\n    }\r\n\r\n    return ActualDelta != 0;\r\n}\r\n```\r\n\r\n---\r\n\r\n## 2. 宿主改造：角色类 (`AHeroYue`)\r\n\r\n现在角色不再拥有血量，而是 **拥有一个血量组件**。\r\n\r\n### A. 头文件 (.h)\r\n```cpp\r\n// 以前：float Health;\r\n// 现在：\r\nprotected:\r\n    UPROPERTY(VisibleAnywhere, BlueprintReadOnly, Category = \"Components\")\r\n    TObjectPtr<class UMyAttributeComponent> AttributeComp;\r\n\r\npublic:\r\n    // 必须提供 Getter，因为 AttributeComp 是 protected 的，别的类 (如 Controller) 需要访问它来绑定 UI\r\n    FORCEINLINE UMyAttributeComponent* GetAttributeComp() const { return AttributeComp; }\r\n```\r\n\r\n### B. 构造函数 (.cpp)\r\n```cpp\r\nAHeroYue::AHeroYue()\r\n{\r\n    // 创建组件。这行代码决定了你的角色拥有了生命值。\r\n    AttributeComp = CreateDefaultSubobject<UMyAttributeComponent>(TEXT(\"AttributeComp\"));\r\n}\r\n```\r\n\r\n### C. 受击逻辑 (`TakeDamage`)\r\n```cpp\r\nfloat AHeroYue::TakeDamage(...)\r\n{\r\n    Super::TakeDamage(...);\r\n\r\n    if (AttributeComp)\r\n    {\r\n        // 角色不再自己做减法，而是把任务“外包”给组件\r\n        // 传入负数 -DamageAmount 表示扣血\r\n        AttributeComp->ApplyHealthChange(-DamageAmount, EventInstigator);\r\n    }\r\n    return DamageAmount;\r\n}\r\n```\r\n\r\n---\r\n\r\n## 3. 表现层：UI 后端 (`UMyPlayerHUD`)\r\n\r\n这是 **MVVM** 模式中的 **View-Model** 层。它负责控制蓝图里的控件。\r\n\r\n### A. 头文件 (.h) —— 绑定魔法\r\n```cpp\r\n#pragma once\r\n#include \"CoreMinimal.h\"\r\n#include \"Blueprint/UserWidget.h\"\r\n#include \"MyPlayerHUD.generated.h\"\r\n\r\nUCLASS()\r\nclass MYGAME_API UMyPlayerHUD : public UUserWidget\r\n{\r\n    GENERATED_BODY()\r\n\r\npublic:\r\n    // 供 Controller 调用的刷新函数\r\n    void UpdateHealth(float CurrentHealth, float MaxHealth);\r\n\r\nprotected:\r\n    // 【核心知识点】meta = (BindWidget)\r\n    // 含义：编译时检查蓝图子类，必须有一个叫 \"HealthBar\" 的控件。\r\n    // 作用：自动把蓝图里的控件指针赋给这个 C++ 变量，省去了 GetWidgetFromName 的开销。\r\n    UPROPERTY(meta = (BindWidget))\r\n    TObjectPtr<class UProgressBar> HealthBar;\r\n};\r\n```\r\n\r\n---\r\n\r\n## 4. 组装层：控制器 (`AMyPlayerController`)\r\n\r\n这是 **大脑**。它负责把 **数据(Component)** 和 **界面(UI)** 连起来。\r\n\r\n### A. 头文件 (.h)\r\n```cpp\r\nprotected:\r\n    virtual void BeginPlay() override;\r\n\r\n    // 这是一个回调函数，用来接收组件的广播\r\n    // 格式必须和 FOnHealthChanged 委托一致！\r\n    UFUNCTION() \r\n    void OnHealthChanged(float NewHealth, float Max, AActor* Instigator, float Delta);\r\n\r\n    // UI 实例\r\n    UPROPERTY()\r\n    TObjectPtr<UMyPlayerHUD> HUDWidget;\r\n\r\n    // UI 模具 (蓝图类)\r\n    UPROPERTY(EditDefaultsOnly, Category=\"UI\")\r\n    TSubclassOf<UUserWidget> HUDClass;\r\n```\r\n\r\n### B. 源文件 (.cpp) —— 逻辑闭环\r\n这段代码是整个系统的 **“总开关”**。\r\n\r\n```cpp\r\nvoid AMyPlayerController::BeginPlay()\r\n{\r\n    Super::BeginPlay();\r\n\r\n    // --- 1. 创建 UI ---\r\n    // IsLocalController() 判断很重要，防止在服务器端创建 UI (服务器没人看，不需要 UI)\r\n    if (IsLocalController() && HUDClass)\r\n    {\r\n        HUDWidget = CreateWidget<UMyPlayerHUD>(this, HUDClass);\r\n        if (HUDWidget)\r\n        {\r\n            HUDWidget->AddToViewport(); // 贴到屏幕上\r\n        }\r\n    }\r\n\r\n    // --- 2. 获取数据源 ---\r\n    AHeroYue* MyPawn = GetPawn<AHeroYue>();\r\n    if (MyPawn)\r\n    {\r\n        UMyAttributeComponent* AttComp = MyPawn->GetAttributeComp();\r\n        if (AttComp)\r\n        {\r\n            // --- 3. 绑定监听 (以后变了告诉我) ---\r\n            AttComp->OnHealthChanged.AddDynamic(this, &AMyPlayerController::OnHealthChanged);\r\n\r\n            // --- 4. 强制初始化 (现在就刷新一次) ---\r\n            // 为什么？因为委托只在\"变化\"时触发。刚进游戏是满血，没变化，UI 会显示默认值(可能不准)。\r\n            // 所以要手动推一次数据。\r\n            OnHealthChanged(AttComp->GetHealth(), AttComp->GetMaxHealth(), nullptr, 0.0f);\r\n        }\r\n    }\r\n}\r\n\r\n// 收到通知后的反应\r\nvoid AMyPlayerController::OnHealthChanged(float NewHealth, float Max, AActor* Instigator, float Delta)\r\n{\r\n    if (HUDWidget)\r\n    {\r\n        // 转发给 UI 去显示\r\n        HUDWidget->UpdateHealth(NewHealth, Max);\r\n    }\r\n}\r\n```\r\n\r\n---\r\n\r\n### 5. 避坑指南 (Troubleshooting)\r\n\r\n1.  **UI 不显示？**\r\n    *   检查 `HUDClass` 是否在 GameMode 或 Controller 蓝图里赋值了。\r\n    *   检查 `AddToViewport` 调了吗？\r\n2.  **崩溃 (Crash)？**\r\n    *   **BindWidget 名字不对**：C++ 叫 `HealthBar`，蓝图里叫 `ProgressBar_123` -> **必崩**。\r\n    *   **空指针**：`HUDWidget` 没创建成功就调用 `UpdateHealth`。\r\n3.  **血条反向 (加血变扣血)？**\r\n    *   检查 `ApplyHealthChange` 传入的是正数还是负数。\r\n    *   检查 `UpdateHealth` 里 `Current / Max` 的数学逻辑。\n","ue_011":"# UE 笔记 011：动画系统与表现架构 (Animation Programming)\r\n**创建时间：** 2025年12月22日\r\n**核心目标：** 用 C++ 驱动逻辑，用蓝图处理表现，实现行走、跳跃、射击的分层混合。\r\n\r\n---\r\n\r\n## 1. 动画架构：C++ 驱动 (AnimInstance)\r\n**核心原则：** **Logic in C++, Graph in Blueprint.** (逻辑在C++，连线在蓝图)。拒绝在蓝图 Update 中写复杂运算。\r\n\r\n### A. 核心类：`UAnimInstance`\r\n不要直接用引擎默认的动画蓝图父类，必须创建自己的 C++ 子类。\r\n\r\n### B. 关键函数 (生命周期)\r\n*   **`NativeInitializeAnimation`**：(对应 `BeginPlay`)\r\n    *   **用途**：缓存 `Pawn` 和 `MovementComponent` 指针。避免每帧 `Cast`，极大提升性能。\r\n*   **`NativeUpdateAnimation`**：(对应 `Tick`)\r\n    *   **用途**：每帧计算变量（速度、空中状态、加速度）。\r\n    *   **Fast Path 优化**：C++ 算好变量，蓝图只负责读取变量驱动状态机，能触发引擎底层的多线程动画优化。\r\n\r\n### C. 代码模版\r\n```cpp\r\n// .h\r\nUPROPERTY(BlueprintReadOnly, Category = \"Animation\")\r\nfloat GroundSpeed;\r\n\r\nUPROPERTY(BlueprintReadOnly, Category = \"Animation\")\r\nbool bIsFalling;\r\n\r\n// .cpp\r\nvoid UHeroAnimInstance::NativeUpdateAnimation(float DeltaTime)\r\n{\r\n    Super::NativeUpdateAnimation(DeltaTime);\r\n    if (!MovementComp) return;\r\n\r\n    // 1. 计算地面速度 (忽略 Z 轴)\r\n    GroundSpeed = MovementComp->Velocity.Size2D();\r\n\r\n    // 2. 获取状态\r\n    bIsFalling = MovementComp->IsFalling();\r\n}\r\n```\r\n\r\n---\r\n\r\n## 2. 运动系统 (Locomotion)\r\n**核心原则：** 状态机管理流程，混合空间管理程度。\r\n\r\n### A. 混合空间 (Blend Space 1D)\r\n*   **用途**：根据一个变量（Speed）在多个动画（Idle -> Walk -> Run）之间平滑过渡。\r\n*   **设置**：Horizontal Axis 设为 `Speed`，最大值设为角色最大速度（如 600）。\r\n\r\n### B. 状态机 (State Machine)\r\n*   **Idle/Run**：放入混合空间。\r\n*   **Jump 流程**：`Start` (起跳) -> `Loop` (滞空) -> `End` (落地)。\r\n*   **切换规则**：\r\n    *   **起跳**：检测 `bIsFalling` 为 true。\r\n    *   **落地**：检测 `bIsFalling` 为 false。\r\n    *   **动作衔接**：`Start -> Loop` 使用 **Time Remaining < 0.1** (播完切)；`Loop -> End` **不要**检测时间 (落地瞬间必须切)。\r\n\r\n---\r\n\r\n## 3. 战斗表现：蒙太奇 (Montage)\r\n**核心原则：** 一次性动作（攻击、受击、换弹）脱离状态机，独立播放。\r\n\r\n### A. 核心资产配置\r\n1.  **创建**：右键动画序列 -> Create AnimMontage。\r\n2.  **插槽 (Slot)**：**必须配置！** 默认是 `DefaultSlot`，如果要分层混合需改为 `UpperBody`。\r\n3.  **循环 (Section)**：如果需要持续射击，在 Montage Sections 里设置 `Loop` 段落自循环。\r\n\r\n### B. C++ 播放逻辑\r\n*   **播放**：`PlayAnimMontage(MontageAsset)`。\r\n*   **停止**：`StopAnimMontage()` 或 `Montage_Stop()`。\r\n*   **跳转**：`Montage_JumpToSection(FName(\"End\"))` (用于松开按键停止射击)。\r\n\r\n---\r\n\r\n## 4. 高级技巧：分层动画 (Layered Animation)\r\n**核心原则：** 上半身开枪，下半身跑步。解决“滑步射击”问题。\r\n\r\n### A. AnimGraph 节点结构\r\ngraph LR\r\nA[跑步状态机(Cache)] -->|Base Pose| B[Layered Blend per bone]\r\nA -->|Slot: UpperBody| B\r\nB --> Output\r\n\r\n### B. 关键设置\r\n1.  **Slot 节点**：在 AnimGraph 中必须添加 `Slot 'UpperBody'` 节点，否则蒙太奇发出的指令没人接收。\r\n2.  **混合滤镜 (Filters)**：\r\n    *   **Bone Name**: `spine_01` (从脊柱开始切分)。\r\n    *   **Mesh Space Rotation**: **勾选 ** (让上半身跟随下半身旋转，否则身体会拧麻花)。\r\n3.  **缓存姿势 (Cached Pose)**：\r\n    *   为了防止没播放蒙太奇时上半身变成 T-Pose，必须把跑步状态机 **缓存 (Save Cached Pose)**。\r\n    *   一份给 Base Pose（底座）。\r\n    *   一份给 Layer Pose（如果没蒙太奇，就透传跑步动作）。\r\n\r\n---\r\n\r\n## 5. 美术管线：IK 重定向 (Retargeting)\r\n**核心原则：** 只要有 IK Rig，任何模型的动画都能通用。\r\n\r\n### A. 核心流程\r\n1.  **定义骨骼 (IK Rig)**：给源模型（如 UE4 小白人）和目标模型（你的角色）分别建立 IK Rig。\r\n2.  **定义链条 (Chains)**：手动圈选 `Spine`, `LeftArm`, `Head` 等关键部位。**名字必须一致**。\r\n3.  **重定向器 (IK Retargeter)**：将两者配对。\r\n4.  **姿势对齐 (Edit Pose)**：**最重要一步**。调整目标模型的关节角度，使其与源模型的 T-Pose 或 A-Pose 保持一致，否则导出的动画胳膊会穿模。\r\n\r\n### B. 根运动陷阱 (Root Motion)\r\n*   **现象**：播放射击动画时，角色莫名其妙往前走，或者脱离胶囊体。\r\n*   **原因**：动画素材自带了 `Pelvis` 或 `Root` 的位移。\r\n*   **解法**：\r\n    *   **方案一 (推荐)**：打开动画序列 -> 勾选 **`Force Root Lock`** (强制锁死根骨骼)。\r\n    *   **方案二**：修改骨骼的 Translation Retargeting 选项，将 Pelvis 设为 `Skeleton` (死也不动)。\r\n\r\n---\r\n\r\n## 6. 常见报错与调试\r\n1.  **蒙太奇不播放**：AnimGraph 里忘了加 `Slot` 节点。\r\n2.  **上半身僵直**：分层混合时，Blend Pose 没有连接 Cached Pose 的保底动作。\r\n3.  **C++ 崩溃**：`NativeUpdateAnimation` 里访问成员变量前，没有检查 `if (MyCharacter)` 和 `if (MovementComp)`。\r\n\r\n---\n","ue_012":"# UE 笔记 012：AI 系统架构与行为树开发 \r\n**创建时间：** 2025年12月26日\r\n**核心目标：** 从零搭建 AI 基础设施，深刻理解行为树运行机制，掌握 C++ 扩展 AI 逻辑的标准流程。\r\n\r\n---\r\n\r\n## 1. AI 的基础设施 (Infrastructure)\r\n**核心原理：** AI 是瞎子和瘸子，必须由我们提供“地图”和“大脑”。\r\n\r\n### A. 导航网格 (NavMesh) —— AI 的路\r\n*   **本质**：引擎将场景中的静态几何体（Static Mesh）进行体素化分析，生成的一张“可行走区域”的 2D 网格数据。\r\n*   **操作步骤**：\r\n    1.  拖入 `NavMeshBoundsVolume`。\r\n    2.  **缩放 (Scale)**：必须完全包裹住地面和 AI 可能去的地方（包括空中，如果是飞行 AI）。\r\n    3.  **调试**：按 **`P`** 键。\r\n        *   **绿色** = 可行走。\r\n        *   **红色** = 阻挡/不可达。\r\n*   **运行时动态构建 (Runtime Generation)**：\r\n    *   **Static (默认)**：游戏打包后网格不再变化。省性能。\r\n    *   **Dynamic**：游戏里门开了/桥断了，网格会自动重算。需在 `Project Settings -> Navigation Mesh` 中开启。\r\n\r\n### B. AI 控制器 (`AAIController`) —— AI 的大脑\r\n*   **区别**：\r\n    *   `APlayerController`：处理 **输入 (Input)**，服务于人类。\r\n    *   `AAIController`：处理 **感知与决策 (Perception & Decision)**，服务于电脑。\r\n*   **附身规则 (`AutoPossessAI`)**：\r\n    在 `ACharacter` 的构造函数中必须设置，否则 AI 可能是植物人。\r\n    ```cpp\r\n    // 告诉引擎：用我自己写的这个 AI 控制器\r\n    AIControllerClass = AMyAIController::StaticClass();\r\n    \r\n    // 告诉引擎：无论是我把怪拖进场景(Placed)，还是代码生成怪(Spawned)，都要给它装上脑子\r\n    AutoPossessAI = EAutoPossessAI::PlacedInWorldOrSpawned;\r\n    ```\r\n\r\n---\r\n\r\n## 2. 行为树体系 (Behavior Tree System)\r\n**核心原理：** 数据与逻辑分离 (Data-Driven)。\r\n\r\n### A. 黑板 (Blackboard / BB) —— **存数据**\r\n*   **定义**：AI 的“内存条”。它是一个 Key-Value 的数据库。\r\n*   **常用类型**：\r\n    *   `Object` (存 Actor，如玩家、攻击目标)。\r\n    *   `Vector` (存坐标，如巡逻点)。\r\n    *   `Bool` (存状态，如是否受伤)。\r\n    *   `Enum` (存状态机，如 巡逻/战斗/逃跑)。\r\n\r\n### B. 行为树 (Behavior Tree / BT) —— **做决策**\r\n*   **定义**：AI 的“流程图”。它每帧（或事件驱动）扫描树的节点，决定做什么。\r\n*   **核心节点**：\r\n    *   **Composites (复合节点)**：\r\n        *   **`Sequence` (->)**：**全真则真，一假即停**。按顺序执行，全成功才算成功。适合“走过去 -> 攻击 -> 播放动画”这种连续动作。\r\n        *   **`Selector` (?)**：**一真即停，全假才假**。按顺序尝试，只要有一个成功了就不继续往下看。适合“优先攻击，不行就巡逻，再不行就发呆”这种分支逻辑。\r\n    *   **Tasks (任务节点)**：**叶子节点**。真正干活的（移动、攻击、等）。\r\n\r\n---\r\n\r\n## 3. C++ 扩展行为树 (Custom BTTask)\r\n**核心原理：** 蓝图节点不够用时，用 C++ 写底层逻辑，暴露给策划连线。\r\n\r\n### A. 模块依赖 (Build.cs)\r\n这是新手必崩点。行为树和导航系统不在核心模块里。\r\n```csharp\r\nPublicDependencyModuleNames.AddRange(new string[] { \r\n    \"Core\", \"CoreUObject\", \"Engine\", \"InputCore\",\r\n    \"AIModule\",        // 行为树、黑板的核心\r\n    \"GameplayTasks\",   // 任务系统\r\n    \"NavigationSystem\" // 寻路系统 (UNavigationSystemV1)\r\n});\r\n```\r\n\r\n### B. 头文件写法 (.h)\r\n```cpp\r\n#pragma once\r\n#include \"CoreMinimal.h\"\r\n// 继承 BlackboardBase，系统会自动送你一个 \"BlackboardKey\" 变量，方便在编辑器里选 Key\r\n#include \"BehaviorTree/Tasks/BTTask_BlackboardBase.h\" \r\n#include \"BTTask_FindRandomLocation.generated.h\"\r\n\r\nUCLASS()\r\nclass MYGAME_API UBTTask_FindRandomLocation : public UBTTask_BlackboardBase\r\n{\r\n    GENERATED_BODY()\r\npublic:\r\n    UBTTask_FindRandomLocation();\r\n\r\n    // 重写 ExecuteTask (相当于 BeginPlay)\r\n    // 只要行为树跑到这个节点，就会执行这个函数\r\n    virtual EBTNodeResult::Type ExecuteTask(UBehaviorTreeComponent& OwnerComp, uint8* NodeMemory) override;\r\n\r\nprotected:\r\n    // 暴露参数：巡逻半径\r\n    UPROPERTY(EditAnywhere, Category = \"AI\")\r\n    float Radius = 1000.0f;\r\n};\r\n```\r\n\r\n### C. 源文件写法 (.cpp)\r\n```cpp\r\n#include \"BTTask_FindRandomLocation.h\"\r\n#include \"AIController.h\"\r\n#include \"NavigationSystem.h\" // 必须引用，否则找不到 NavSys\r\n#include \"BehaviorTree/BlackboardComponent.h\" // 必须引用，否则无法 SetValue\r\n\r\nUBTTask_FindRandomLocation::UBTTask_FindRandomLocation()\r\n{\r\n    // 设置节点显示名称 (拖出来后看到的名字)\r\n    NodeName = TEXT(\"Find Random Location\");\r\n}\r\n\r\nEBTNodeResult::Type UBTTask_FindRandomLocation::ExecuteTask(UBehaviorTreeComponent& OwnerComp, uint8* NodeMemory)\r\n{\r\n    // 1. 获取控制权：拿到 Controller 和 Pawn\r\n    AAIController* AICon = OwnerComp.GetAIOwner();\r\n    APawn* MyPawn = AICon ? AICon->GetPawn() : nullptr;\r\n    if (!MyPawn) return EBTNodeResult::Failed;\r\n\r\n    // 2. 获取导航系统单例 (GetWorld)\r\n    UNavigationSystemV1* NavSys = UNavigationSystemV1::GetCurrent(GetWorld());\r\n    if (!NavSys) return EBTNodeResult::Failed;\r\n\r\n    // 3. 执行寻路算法：找半径内的一个随机可达点\r\n    FNavLocation ResultLoc;\r\n    bool bFound = NavSys->GetRandomReachablePointInRadius(\r\n        MyPawn->GetActorLocation(), // 中心点\r\n        Radius,                     // 半径\r\n        ResultLoc                   // 输出结果\r\n    );\r\n\r\n    // 4. 写入黑板 (关键步骤)\r\n    if (bFound)\r\n    {\r\n        // GetSelectedBlackboardKey() 是父类提供的，它读取你在编辑器下拉菜单里选的那个 Key\r\n        OwnerComp.GetBlackboardComponent()->SetValueAsVector(GetSelectedBlackboardKey(), ResultLoc.Location);\r\n        \r\n        // 5. 汇报成功 (Succeeded)\r\n        // 告诉行为树：这事我办成了，请继续往下走\r\n        return EBTNodeResult::Succeeded;\r\n    }\r\n\r\n    // 6. 汇报失败 (Failed)\r\n    // 告诉行为树：这事办砸了，Sequence 会中断\r\n    return EBTNodeResult::Failed;\r\n}\r\n```\r\n\r\n---\r\n\r\n## 4. 链接 C++ 与 编辑器 (The Link)\r\n\r\n这是最容易让人晕的地方：**代码里的变量是怎么变成编辑器里的选项的？**\r\n\r\n### A. 键的绑定 (Key Selector)\r\n*   **代码**：`UBTTask_BlackboardBase` 内部有一个 `FBlackboardKeySelector BlackboardKey;`\r\n*   **编辑器**：你在 Details 面板里，把 `BlackboardKey` 设为 `PatrolLocation`。\r\n*   **运行时**：`GetSelectedBlackboardKey()` 获取到了 `PatrolLocation` 的内存 ID。\r\n*   **操作**：`SetValueAsVector` 根据 ID 把数据填入黑板。\r\n\r\n### B. 任务的查找 (Task Finding)\r\n*   **右键菜单**：显示的是 **C++ 类名** (UE 会自动去掉前缀 UBTTask_ 并加空格)。\r\n*   **节点标题**：显示的是 **`NodeName`**。\r\n*   **如果找不到**：通常是 Live Coding 缓存没更新。**关闭编辑器 -> Rider Build -> 重启** 必解。\r\n\r\n---\r\n\r\n## 5. 避坑指南 (Troubleshooting)\r\n\r\n1.  **AI 站着不动**：\r\n    *   检查 NavMesh 是否变绿？\r\n    *   检查 `AutoPossessAI` 是否为 `PlacedInWorldOrSpawned`？\r\n    *   检查行为树是否 `RunBehaviorTree` 成功？\r\n2.  **编译报错 (Linker Error)**：\r\n    *   检查 `Build.cs` 是否加了 `NavigationSystem` 和 `GameplayTasks`。\r\n    *   检查 `.cpp` 是否 `#include \"NavigationSystem.h\"`。\r\n3.  **黑板键选不了**：\r\n    *   检查黑板里的 Key 类型（Vector）和 C++ 代码里写的函数 (`SetValueAsVector`) 是否匹配。\n","ue_013":"# UE 笔记 013：AI 感知系统与动态决策 (Perception & Decision)\r\n**创建时间：** 2025年12月27日\r\n**核心目标：** 给 AI 装上眼睛，实现“看见玩家 -> 打断巡逻 -> 追击 -> 跟丢恢复”的完整闭环。\r\n\r\n---\r\n\r\n## 1. 感知系统架构 (AI Perception System)\r\n**核心原则：** 硬件（Component）+ 参数（Config）+ 信号（Delegate）。\r\n\r\n### A. 必要的头文件 (IWYU)\r\n感知系统不在 Core 模块里，必须显式引用，否则报“无法解析符号”。\r\n```cpp\r\n#include \"Perception/AIPerceptionComponent.h\"\r\n#include \"Perception/AISenseConfig_Sight.h\" // 必须加！否则找不到 SightRadius\r\n```\r\n\r\n### B. 构造函数配置标准模版\r\n**注意：** `DetectionByAffiliation` 必须全开，否则 AI 默认不看没有定义阵营的玩家。\r\n\r\n```cpp\r\nAMyAIController::AMyAIController()\r\n{\r\n    // 1. 创建组件\r\n    PerceptionComp = CreateDefaultSubobject<UAIPerceptionComponent>(TEXT(\"PerceptionComp\"));\r\n    SightConfig = CreateDefaultSubobject<UAISenseConfig_Sight>(TEXT(\"SightConfig\"));\r\n\r\n    if (SightConfig)\r\n    {\r\n        // 2. 视力参数\r\n        SightConfig->SightRadius = 1500.0f;           // 看多远\r\n        SightConfig->LoseSightRadius = 2000.0f;       // 跟丢缓冲距离 (防止边缘抖动)\r\n        SightConfig->PeripheralVisionAngleDegrees = 90.0f; // 视野角度\r\n\r\n        // 3. 【绝对核心】阵营检测 (三项全勾，防止装瞎)\r\n        SightConfig->DetectionByAffiliation.bDetectEnemies = true;\r\n        SightConfig->DetectionByAffiliation.bDetectNeutrals = true;\r\n        SightConfig->DetectionByAffiliation.bDetectFriendlies = true;\r\n    }\r\n\r\n    // 4. 注册并应用\r\n    if (PerceptionComp)\r\n    {\r\n        PerceptionComp->ConfigureSense(*SightConfig); // 注意传的是引用(*ptr)\r\n        PerceptionComp->SetDominantSense(SightConfig->GetSenseImplementation());\r\n    }\r\n}\r\n```\r\n\r\n---\r\n\r\n## 2. 信号处理逻辑 (Stimulus)\r\n**核心原则：** 函数调用 = 状态改变；Stimulus = 当前状态。\r\n\r\n### A. 绑定委托\r\n在 `BeginPlay` 中绑定 `OnTargetPerceptionUpdated`。\r\n\r\n### B. 回调逻辑解析\r\n```cpp\r\nvoid AMyAIController::OnTargetDetected(AActor* Actor, FAIStimulus Stimulus)\r\n{\r\n    // 1. 过滤目标 (只追 Pawn)\r\n    if (auto* SensedPawn = Cast<APawn>(Actor))\r\n    {\r\n        // 2. 判断是\"进\"还是\"出\"\r\n        if (Stimulus.WasSuccessfullySensed())\r\n        {\r\n            // Enter: 看见了 -> 写入黑板 TargetActor\r\n            GetBlackboardComponent()->SetValueAsObject(\"TargetActor\", Actor);\r\n        }\r\n        else\r\n        {\r\n            // Exit: 跟丢了 -> 清空黑板 (或记录最后位置)\r\n            GetBlackboardComponent()->SetValueAsObject(\"TargetActor\", nullptr);\r\n        }\r\n    }\r\n}\r\n```\r\n\r\n---\r\n\r\n## 3. 行为树决策逻辑 (Behavior Tree Logic)\r\n**核心原则：** 利用 **Selector (选择器)** 的优先级和 **Decorator (装饰器)** 的中断机制。\r\n\r\n### A. 树形结构\r\ngraph TD\r\n    Root --> Selector[Selector (AI Root)]\r\n    Selector -->|优先| ChaseSeq[Sequence (追击)]\r\n    Selector -->|保底| PatrolSeq[Sequence (巡逻)]\r\n\r\n### B. 装饰器配置 (Blackboard Decorator)\r\n放在 **Chase Sequence** 上，作为“守门员”。\r\n*   **Blackboard Key**: `TargetActor`\r\n*   **Key Query**: **Is Set** (有值就让进，没值就踢走)。\r\n*   **Observer Aborts (关键)**: **Both** (建议) 或 **Lower Priority**。\r\n    *   *含义*：一旦 `TargetActor` 有值了，立刻打断右边的巡逻，切过来；一旦值空了，立刻打断追击，切回去。\r\n\r\n---\r\n\r\n## 4. 移动手感调优 (Locomotion Tuning)\r\n**核心原则：** 身体听移动组件的，别听控制器的瞬间指令。\r\n\r\n### A. 解决“瞬移转身”\r\nAI 转身生硬是因为 `Controller` 瞬间改变了朝向，强迫 `Pawn` 跟随。\r\n\r\n**在 AI Character 构造函数中设置：**\r\n```cpp\r\n// 1. 切断大脑控制\r\nbUseControllerRotationYaw = false; \r\n\r\n// 2. 启用物理平滑旋转\r\nGetCharacterMovement()->bOrientRotationToMovement = true;\r\n\r\n// 3. 调整速度 (Degrees Per Second)\r\nGetCharacterMovement()->RotationRate = FRotator(0.0f, 600.0f, 0.0f);\r\n```\r\n\r\n### B. 解决“站桩不转身”\r\n如果 AI 需要原地盯着玩家 (SetFocus) 而不移动：\r\n```cpp\r\nbUseControllerRotationYaw = false;\r\nGetCharacterMovement()->bOrientRotationToMovement = false; \r\n// 开启\"期望旋转\"\r\nGetCharacterMovement()->bUseControllerDesiredRotation = true; \r\n```\r\n\r\n---\r\n\r\n## 5. 调试与排错 (Debug Checklist)\r\n\r\n1.  **AI 瞎子 (看不见)**：\r\n    *   检查 **DetectionByAffiliation** 是否三项全 `true`。\r\n    *   检查 `SightRadius` 是否太短。\r\n2.  **AI 甚至不生成感知组件**：\r\n    *   检查构造函数里是否漏写了 `CreateDefaultSubobject`。\r\n    *   **崩溃 (0xC0000005)**：通常是因为先使用了 `SightConfig` 指针，后才创建它（顺序反了）。\r\n3.  **调试工具**：\r\n    *   运行游戏时按 **`'` (单引号)** 键。\r\n    *   **绿球**：看见了。\r\n    *   **红球**：跟丢了（记忆位置）。\r\n    *   **TargetActor: None**：黑板没更新，检查 C++ 逻辑。\r\n\r\n---\n","ue_014":"# UE 笔记 014：AI 战斗执行与数据驱动入门\r\n**创建时间：** 2025年12月28日\r\n**核心目标：** 实现 AI 的近战攻击闭环，建立基于 DataAsset 的物品配置系统。\r\n\r\n---\r\n\r\n## 1. AI 战斗执行端 (AI Combat Execution)\r\n**核心原则：** 行为树只负责“扣扳机”，角色负责“开枪”。\r\n\r\n### A. 自定义攻击任务 (`BTTask_Attack`)\r\n*   **职责**：连接 **行为树** 与 **角色 C++ 逻辑** 的桥梁。\r\n*   **父类**：`UBTTaskNode` (不需要读写黑板时用这个，比 BlackboardBase 轻量)。\r\n*   **核心代码**：\r\n    ```cpp\r\n    EBTNodeResult::Type UBTTask_Attack::ExecuteTask(...)\r\n    {\r\n        // 1. 拿到 Controller -> 拿到 Pawn -> 强转为 AMyEnemy\r\n        // 2. 调用 Enemy->Attack();\r\n        // 3. 立即返回 Succeeded (射后不理，冷却由行为树的 Wait 节点控制)\r\n        return EBTNodeResult::Succeeded;\r\n    }\r\n    ```\r\n\r\n### B. 角色攻击逻辑 (`AMyEnemy::Attack`)\r\n*   **职责**：播放蒙太奇 (Montage)。\r\n*   **防鬼畜优化**：\r\n    ```cpp\r\n    // 防止上一次还没播完，下一次又重头播，导致抽搐\r\n    if (!AnimInstance->Montage_IsPlaying(AttackMontage))\r\n    {\r\n        PlayAnimMontage(AttackMontage);\r\n    }\r\n    ```\r\n\r\n---\r\n\r\n## 2. 近战伤害判定系统 (Melee Hitbox)\r\n**核心原则：** 动画驱动逻辑 (AnimNotify)。**精准控制**伤害判定的开启与关闭。\r\n\r\n### A. 组件准备\r\n*   **`UBoxComponent`**：绑定在手部或武器骨骼插槽 (`Socket`) 上。\r\n*   **默认状态**：`NoCollision` (必须关闭，防止走路撞人)。\r\n*   **开启状态**：`QueryOnly` + `Overlap` (仅检测重叠)。\r\n\r\n### B. 动画通知 (AnimNotify)\r\n*   **作用**：在蒙太奇的特定帧（挥刀、收刀）触发 C++ 逻辑。\r\n*   **实现方式**：\r\n    *   **C++ 方式**：创建 `UNotify_EnableHit` 类，重写 `Notify` 函数。\r\n    *   **蓝图方式**：在动画蓝图 EventGraph 里搜索 `Event AnimNotify_xxx`，调用 C++ 的 `SetMeleeCollisionEnabled` 函数。\r\n\r\n### C. 伤害去重逻辑 (De-duplication) —— **重点考点**\r\n*   **问题**：挥刀过程持续 0.5 秒，Overlap 会触发几十次，导致瞬间秒杀玩家。\r\n*   **解决**：使用 **`TArray<AActor*> HitVictims`** 名单。\r\n    1.  **开启时** (`Notify_Enable`)：`HitVictims.Empty()` 清空名单。\r\n    2.  **重叠时** (`OnOverlap`)：\r\n        ```cpp\r\n        // 只有名单里没有他，才造成伤害\r\n        if (!HitVictims.Contains(OtherActor)) \r\n        {\r\n            HitVictims.Add(OtherActor); // 记下来\r\n            UGameplayStatics::ApplyDamage(...); // 扣血\r\n        }\r\n        ```\r\n    3.  **关闭时** (`Notify_Disable`)：关闭碰撞。\r\n\r\n---\r\n\r\n## 3. 数据驱动基础 (Data-Driven Framework)\r\n**核心原则：** 逻辑写在 C++ (Class)，数据填在编辑器 (Asset)。\r\n\r\n### A. 数据资产 (`UDataAsset`)\r\n这是替代“硬编码”的神器。不要在代码里写 `Damage = 10`。\r\n*   **创建**：继承自 **`UPrimaryDataAsset`**。\r\n*   **内容**：只定义变量，不写逻辑。\r\n    ```cpp\r\n    UCLASS()\r\n    class UItemData : public UPrimaryDataAsset\r\n    {\r\n        GENERATED_BODY()\r\n    public:\r\n        UPROPERTY(EditDefaultsOnly, BlueprintReadOnly, Category=\"Data\")\r\n        EItemType ItemType;\r\n        \r\n        UPROPERTY(EditDefaultsOnly, BlueprintReadOnly, Category=\"Data\")\r\n        UStaticMesh* PickupMesh;\r\n        \r\n        UPROPERTY(EditDefaultsOnly, BlueprintReadOnly, Category=\"Data\")\r\n        UTexture2D* Icon;\r\n    };\r\n    ```\r\n*   **使用**：在编辑器里右键 -> Miscellaneous -> Data Asset -> 创建 `DA_Sword`。\r\n\r\n### B. 自动配置 Actor (`OnConstruction`)\r\n如何让一个 Actor 拖进场景后，根据选的 DataAsset 自动变样子？\r\n*   **函数**：**`OnConstruction`** (相当于蓝图的 Construction Script)。\r\n*   **逻辑**：\r\n    ```cpp\r\n    void AItemPickup::OnConstruction(const FTransform& Transform)\r\n    {\r\n        Super::OnConstruction(Transform);\r\n        // 如果策划选了数据，立马刷新模型显示\r\n        if (ItemInfo && ItemInfo->PickupMesh)\r\n        {\r\n            MeshComp->SetStaticMesh(ItemInfo->PickupMesh);\r\n        }\r\n    }\r\n    ```\r\n\r\n---\r\n\r\n## 4. 关键排坑指南 (Troubleshooting)\r\n\r\n### A. 动画播放失败\r\n*   **症状**：代码跑了，Log 打印了，但人不动。\r\n*   **病因**：动画蓝图 (AnimBP) 里没有加 **`Slot 'DefaultSlot'`** (或者 `UpperBody`) 节点。\r\n*   **原理**：蒙太奇必须通过 Slot 插入到姿势流中，否则会被状态机覆盖。\r\n\r\n### B. 伤害判定失效\r\n*   **症状**：刀砍过去了，穿过去了，没扣血。\r\n*   **病因 1**：逻辑写反了。写成了 `if (Contains)` (包含才打) 而不是 `if (!Contains)` (不包含才打)。\r\n*   **病因 2**：碰撞预设不对。Box 必须能 Overlap Pawn，且 Pawn 必须开启 `Generate Overlap Events`。\r\n\r\n### C. 数据资产无法赋值\r\n*   **症状**：C++ 写了变量，蓝图里选不了 DataAsset。\r\n*   **病因**：变量类型写成了 `UClass*` 或 `TSubclassOf`。\r\n*   **修正**：应该写 **`TObjectPtr<UItemData>`** (这是实例引用，不是类引用)。\r\n\r\n---\n","ue_015":"# UE 笔记 015：数据驱动架构与背包系统 (详解版)\r\n**归档日期：** 2025年12月31日\r\n**核心里程碑：** 从“硬编码逻辑”转型为“数据驱动架构”，实现了完整的物品产出、存储、使用闭环。\r\n\r\n---\r\n\r\n## 1. 数据驱动核心 (Data-Driven Architecture)\r\n**核心思想：** 代码定义“规则”，资产定义“内容”。\r\n\r\n### A. 为什么选择 `UPrimaryDataAsset`？\r\n*   **对比 DataTable**：表格适合存海量纯数值（Excel），但不方便引用模型/贴图资源，且不支持继承多态。\r\n*   **对比 Actor**：数据不需要实体（坐标/碰撞），DataAsset 只是纯粹的信息载体，内存占用极低。\r\n*   **优势**：支持 **多态 (Polymorphism)**。`UItemData_Potion` 和 `UItemData_Weapon` 可以共存于同一个 `TArray<UItemData*>` 数组中。\r\n\r\n### B. 物品数据基类 (`UItemData`)\r\n这是所有物品的“逻辑所有者”。\r\n\r\n```cpp\r\n// .h\r\nUCLASS()\r\nclass MYGAME_API UItemData : public UPrimaryDataAsset\r\n{\r\n    GENERATED_BODY()\r\n\r\npublic:\r\n    // 基础属性：给 UI 和 掉落物 使用\r\n    UPROPERTY(EditDefaultsOnly, BlueprintReadOnly, Category=\"Data\")\r\n    EItemType ItemType; // 枚举：消耗品/武器/任务\r\n\r\n    UPROPERTY(EditDefaultsOnly, BlueprintReadOnly, Category=\"Data\")\r\n    FText ItemName;     // 物品名\r\n\r\n    UPROPERTY(EditDefaultsOnly, BlueprintReadOnly, Category=\"Data\")\r\n    TObjectPtr<UTexture2D> Icon; // UI 图标\r\n\r\n    UPROPERTY(EditDefaultsOnly, BlueprintReadOnly, Category=\"Data\")\r\n    TObjectPtr<UStaticMesh> PickupMesh; // 掉落模型\r\n\r\n    // 【核心架构】多态使用接口\r\n    // 定义为 NativeEvent，允许 C++ 写默认逻辑，蓝图也能重写\r\n    // 返回 bool：true 代表物品已消耗(需要从背包删除)，false 代表保留\r\n    UFUNCTION(BlueprintNativeEvent, BlueprintCallable, Category=\"Item\")\r\n    bool OnUse(AActor* User);\r\n    \r\n    virtual bool OnUse_Implementation(AActor* User); // C++ 实现体必须加 virtual\r\n};\r\n```\r\n\r\n---\r\n\r\n## 2. 万能拾取物 (`AItemPickup`)\r\n**核心思想：** 一个 Actor 类，扮演无数种物品。\r\n\r\n### A. 构造脚本 (`OnConstruction`)\r\n这是编辑器的魔法。当你修改 `ItemInfo` 变量时，这个函数会立即运行。\r\n*   **逻辑**：读取 `ItemInfo` 里的 `PickupMesh` -> 设置给 `StaticMeshComponent`。\r\n*   **结果**：拖入 `DA_Sword` 变剑，拖入 `DA_Potion` 变药瓶。无需运行游戏即可看到效果。\r\n\r\n### B. 交互实现 (`IInteractInterface`)\r\n*   **重写 `Interact`**：调用 `Instigator->Inventory->AddItem(ItemInfo)`。\r\n*   **销毁**：交互成功后 `Destroy()` 自身。因为数据已经进了背包，地上的壳子就没用了。\r\n\r\n---\r\n\r\n## 3. 背包组件 (`UInventoryComponent`)\r\n**核心思想：** 这是一个“数据容器”，不处理具体的 UI 显示。\r\n\r\n### A. 存储结构\r\n```cpp\r\n// 必须加 UPROPERTY() 防止 DataAsset 被 GC 回收 (虽然 DataAsset 通常是根集，但这是好习惯)\r\n// TArray 维护物品列表\r\nUPROPERTY(VisibleAnywhere, BlueprintReadOnly)\r\nTArray<TObjectPtr<UItemData>> Content;\r\n```\r\n\r\n### B. 增删逻辑 (关键考点)\r\n*   **添加 (`AddItem`)**：\r\n    1.  `Content.Add(NewItem)`\r\n    2.  `OnInventoryChanged.Broadcast(NewItem)` —— 通知 UI 刷新。\r\n*   **使用 (`UseItem`)**：\r\n    1.  调用 `Item->OnUse(Owner)`。\r\n    2.  如果返回 `true` (消耗品)：调用 **`Content.RemoveSingle(Item)`**。\r\n    3.  **坑点回顾**：之前用了 `Remove`，导致捡了两个药水，喝一个全没了。必须用 `RemoveSingle`。\r\n\r\n---\r\n\r\n## 4. UI 系统实现 (UMG & C++)\r\n**核心思想：** 代码生成 UI，而非手动摆放。\r\n\r\n### A. 单个格子 (`UInventorySlot`)\r\n*   **职责**：持有 `UItemData*` 数据引用。\r\n*   **控件绑定**：\r\n    *   `UPROPERTY(meta=(BindWidget)) UImage* IconImage;`\r\n    *   `UPROPERTY(meta=(BindWidget)) UButton* UseBtn;`\r\n*   **点击逻辑**：\r\n    *   `UseBtn->OnClicked` -> 找到 Character -> 找到 InventoryComponent -> 调用 `UseItem(ItemReference)`。\r\n*   **布局坑点**：`SizeBox` 只能有一个子节点。如果想把 Button 和 Image 叠在一起，必须用 **Overlay** 包裹，或者用 Button 包裹 Image。\r\n\r\n### B. 背包窗口 (`UInventoryWindow`)\r\n*   **职责**：根据数组动态生成格子。\r\n*   **核心控件**：**`UUniformGridPanel`** (均匀网格)。\r\n*   **动态生成算法**：\r\n    ```cpp\r\n    void RefreshInventory(const TArray<UItemData*>& Items)\r\n    {\r\n        GridPanel->ClearChildren(); // 清空旧数据\r\n        for (int i = 0; i < Items.Num(); ++i)\r\n        {\r\n            // 1. 创建 Widget\r\n            UInventorySlot* Slot = CreateWidget<UInventorySlot>(GetOwningPlayer(), SlotClass);\r\n            \r\n            // 2. 注入数据\r\n            Slot->Init(Items[i]);\r\n            \r\n            // 3. 计算行列 (假设一行 4 个)\r\n            int32 Row = i / 4;\r\n            int32 Col = i % 4;\r\n            \r\n            // 4. 放入网格\r\n            GridPanel->AddChildToUniformGrid(Slot, Row, Col);\r\n        }\r\n    }\r\n    ```\r\n\r\n### C. 内存保护 (GC Trap)\r\n*   在 Controller 里持有 `InventoryWidget` 指针时，**必须加 `UPROPERTY()`**。\r\n*   **原因**：UI 是 `UObject`。如果不加宏，GC 系统认为它是垃圾，会不定时回收它。导致下次按 I 键时游戏崩溃。\r\n\r\n---\r\n\r\n## 5. 多态实战 (Polymorphism)\r\n**核心思想：** 背包组件不需要知道物品的具体功能，它只管调用接口。\r\n\r\n### A. 药水逻辑 (`UItemData_Potion`)\r\n*   **继承**：`public UItemData`\r\n*   **重写**：`OnUse_Implementation`\r\n*   **逻辑**：获取 `AttributeComponent` -> 加血 -> 返回 `true` (消耗)。\r\n\r\n### B. 武器逻辑 (`UItemData_Weapon`)\r\n*   **继承**：`public UItemData`\r\n*   **新增**：`TSubclassOf<AWeapon> WeaponClass`\r\n*   **重写**：`OnUse_Implementation`\r\n*   **逻辑**：获取 `Character` -> `EquipWeapon(WeaponClass)` -> 返回 `false` (不消耗)。\r\n\r\n---\r\n\r\n## 6. 常见崩溃与修复 (Crash Log)\r\n在开发过程中，我们遇到了几个经典崩溃：\r\n\r\n1.  **崩溃地址 0x70 (空指针)**：\r\n    *   **场景**：`EquipWeapon` 时，`SpawnActor` 失败返回 `nullptr`，紧接着调用 `AttachToComponent`。\r\n    *   **原因**：`DataAsset` 里的 `WeaponClass` 忘了填（None）。\r\n    *   **修复**：在调用指针前，永远加 `if (CurrentWeapon)`。\r\n2.  **Cast 失败**：\r\n    *   **场景**：`DoInteract` 时直接 `Cast`。\r\n    *   **修复**：使用 `Implements<UInterface>` 检查，或者 `IsValid` 检查。\r\n3.  **UI 消失/崩溃**：\r\n    *   **场景**：UI 打开一会后消失，或者报错。\r\n    *   **修复**：给 `TObjectPtr<UInventoryWindow>` 加上 `UPROPERTY()`。\r\n\r\n---\n","ue_016":"# UE 笔记 016：游戏循环与规则系统 (Game Loop)\r\n**归档日期：** 2025年12月31日\r\n**核心目标：** 建立游戏规则（裁判），管理游戏状态（胜负），实现关卡重置。\r\n\r\n---\r\n\r\n## 1. 核心架构：裁判员 (`AGameModeBase`)\r\n**核心原则：** 规则写在 GameMode 里，因为它只存在于服务器（单机即服务器），拥有最高裁决权。\r\n\r\n### A. 职责\r\n*   **判定胜负**：谁死了？是玩家还是 Boss？\r\n*   **流程控制**：游戏开始、游戏结束、暂停。\r\n*   **默认类配置**：指定 DefaultPawn, PlayerController, HUD 等。\r\n\r\n### B. 关键代码模版\r\n```cpp\r\n// 1. 定义接口：让别人来汇报\r\nvoid AMyGameMode::OnActorKilled(AActor* Victim, AController* Killer)\r\n{\r\n    // 2. 识别受害者\r\n    APawn* PlayerPawn = UGameplayStatics::GetPlayerPawn(this, 0);\r\n    \r\n    if (Victim == PlayerPawn)\r\n    {\r\n        // 3. 执行结束逻辑\r\n        GameOver();\r\n    }\r\n}\r\n```\r\n\r\n### C. 为什么不用单例？\r\nGameMode 在每个关卡（Level）开始时都会重新生成。它是 **关卡级** 的管理者，不是全局（GameInstance）级的。\r\n\r\n---\r\n\r\n## 2. 流程控制：控制器 (`APlayerController`)\r\n**核心原则：** 涉及 **UI 显示**、**鼠标控制**、**关卡跳转** 的逻辑，全部交给 Controller。\r\n\r\n### A. 游戏结束处理 (GameOver)\r\n当 GameMode 判定玩家死亡后，通知 Controller：\r\n\r\n```cpp\r\nvoid AMyPlayerController::ShowGameOver()\r\n{\r\n    // 1. 显示 UI\r\n    if (GameOverWidget) GameOverWidget->AddToViewport();\r\n\r\n    // 2. 停止角色操作 (可选)\r\n    GetPawn()->DisableInput(this);\r\n\r\n    // 3. 【核心】切换输入模式 (Input Mode)\r\n    // 允许鼠标点 UI，不允许键盘控制走路\r\n    FInputModeUIOnly InputMode;\r\n    SetInputMode(InputMode);\r\n    \r\n    // 4. 显示鼠标光标\r\n    bShowMouseCursor = true;\r\n}\r\n```\r\n\r\n### B. 重启关卡 (Restart)\r\n通常绑定在 UI 的“再来一次”按钮上。\r\n\r\n```cpp\r\nvoid AMyPlayerController::RestartLevel()\r\n{\r\n    // 获取当前关卡名并重新打开\r\n    UGameplayStatics::OpenLevel(this, FName(*GetWorld()->GetName()), false);\r\n}\r\n```\r\n\r\n---\r\n\r\n## 3. 触发机制：死亡回调\r\n**核心原则：** 在血量归零的瞬间，通知 GameMode。\r\n\r\n*   **位置**：`UAttributeComponent::ApplyHealthChange`。\r\n*   **获取 GameMode**：\r\n    *   `GetWorld()->GetAuthGameMode<AMyGameMode>()`。\r\n    *   *注意：客户端调用这个会返回 nullptr，但这正好防止了客户端作弊修改游戏结果。*\r\n\r\n---\r\n\r\n## 4. 关键 API 速查表\r\n\r\n| 函数/类               | 作用          | 备注                                                   |\r\n| :-------------------- | :------------ | :----------------------------------------------------- |\r\n| **`GetAuthGameMode`** | 获取当前裁判  | 只在 Server/单机 有效。                                |\r\n| **`SetInputMode`**    | 切换操作模式  | `GameOnly` (玩), `UIOnly` (菜单), `GameAndUI` (背包)。 |\r\n| **`OpenLevel`**       | 切换/重置地图 | 也就是“读图”。                                         |\r\n| **`DisableInput`**    | 禁用输入      | 让角色动弹不得。                                       |\r\n\r\n---\r\n\r\n## 5. 避坑指南 (Troubleshooting)\r\n\r\n1.  **GameMode 不生效**：\r\n    *   **原因**：写了 C++ 类，但没在 **World Settings** 里配置 `GameMode Override`。\r\n    *   **解法**：必须创建一个继承自 C++ 的蓝图 GameMode，并在关卡设置里选它。\r\n2.  **重启后鼠标没了 / 动不了**：\r\n    *   **原因**：`OpenLevel` 重置了世界，但没重置 Controller 的输入模式。\r\n    *   **解法**：在 Controller 的 `BeginPlay` 里，重新调用 `SetInputMode(FInputModeGameOnly)` 和 `bShowMouseCursor = false`。\r\n3.  **UI 无法点击**：\r\n    *   **原因**：没切 InputMode 或者没 ShowMouseCursor。\r\n\r\n---\r\n\r\n**【OVER】**\n","gas_shoot_1":"# UE5 GAS 射击系统笔记\r\n## 第 1 篇：底层架构合体与属性金库搭建 (Core Architecture & Attributes)\r\n\r\n### 一、 核心架构原理：逻辑所有者与表现对象的剥离 (Owner vs Avatar)\r\n在项目的射击/竞技游戏中，角色（Character）会频繁死亡和重生。如果将技能组件直接挂在角色上，死亡时所有的 Buff、冷却时间（CD）、弹药数据都会随着 Actor 的销毁而丢失。\r\n\r\n*   **Owner Actor（逻辑所有者/逻辑所有者）**：**`PlayerState`**。它贯穿整局游戏，即使 Character 死亡它依然存在。负责保管 `AbilitySystemComponent` (ASC) 和 `AttributeSet`。\r\n*   **Avatar Actor（表现化身/表现对象）**：**`Character`**。它是可以被随意丢弃和替换的物理外壳，负责接收输入、播放动画、发起碰撞检测。\r\n\r\n### 二、 网络同步基石：ASC 的初始化与配置\r\n在项目项目中，任何逻辑都必须默认是“多人联机”的。在 `PlayerState` 的构造函数中，ASC 的三行核心配置决定了网络性能的生死：\r\n\r\n1.  **`AbilitySystemComponent->SetIsReplicated(true);`**\r\n    *   *原理*：显式开启组件的属性与 RPC 网络同步。\r\n2.  **`AbilitySystemComponent->SetReplicationMode(EGameplayEffectReplicationMode::Mixed);`**\r\n    *   *原理*：**性能优化的核心**。`Mixed` 模式对“自己（Owner）”同步所有具体的 GE 数据（方便预测和 UI 显示），对“别人（Simulated Proxy）”只同步 GameplayTags 和基础属性，极大地节省了网络带宽。\r\n3.  **`NetUpdateFrequency = 100.0f;`**\r\n    *   *原理*：`PlayerState` 默认的同步频率极低（通常为 1Hz）。在射击游戏中，为了保证血量和子弹扣除的及时反馈，必须将其拉高至 100Hz，确保客户端及时收到服务器的最新数据。\r\n\r\n### 三、 逻辑所有者与表现对象的“握手仪式” (`InitAbilityActorInfo`)\r\nGAS 系统必须知道“谁拥有技能”以及“技能该在谁身上表现”。这个动作必须在 **服务端** 和 **客户端** 双端完成：\r\n\r\n1.  **服务端 (`PossessedBy`)**：当 Controller 附身 Character 时，服务器掌握了控制权，立刻进行 ASC 初始化。\r\n2.  **客户端 (`OnRep_PlayerState`)**：由于网络延迟，客户端 Character 生成时 `PlayerState` 可能还未到达。必须监听 `PlayerState` 的同步回调，一旦收到，立刻在客户端本地完成初始化，否则客户端无法放技能。\r\n    *   *核心代码*：`PS->GetAbilitySystemComponent()->InitAbilityActorInfo(PS, this);`\r\n\r\n### 四、 属性金库的搭建 (`AttributeSet`)\r\n属性集是所有数值逻辑的地基。项目不要在业务逻辑中直接写 `Health -= 10`，所有数值必须通过封装好的结构流转。\r\n\r\n1.  **宏定义与内存结构**：\r\n    *   属性必须是 `FGameplayAttributeData` 类型。它内部包含 **BaseValue（基础值）** 和 **CurrentValue（当前值，受临时 Buff 影响）**。\r\n    *   使用 `ATTRIBUTE_ACCESSORS` 宏自动生成 `Get`、`Set`、`Init` 函数，避免手写导致错误。\r\n2.  **数值的最后防线 (`PreAttributeChange`)**：\r\n    *   *作用*：在任何 GE 尝试修改属性之前，进行数值限幅（Clamping）。\r\n    *   *原理*：无论伤害溢出多少，或者子弹扣除逻辑出现什么 Bug，在这里写下 `NewValue = FMath::Clamp(NewValue, 0.0f, GetMaxAmmo());` 就能保证底层数据的绝对合法，防止溢出。\r\n\r\n### 五、 数据驱动的初始化：摒弃硬编码\r\n项目的 C++ 代码中不要写死初始血量（如 `InitHealth(100)` 通常只做底层兜底）。\r\n*   *流程*：我们在 C++ 暴露一个 `DefaultAttributeEffect` (TSubclassOf) 插槽。策划在蓝图里创建一个 `Instant GE`，将数值覆写（Override）为所需的值。\r\n*   *执行*：在 `PossessedBy` 初始化完成后，服务端调用 `MakeOutgoingSpec` 生成单据，并 `ApplyGameplayEffectSpecToSelf`，将策划配好的初值注入属性集。\n","gas_shoot_2":"# UE5 GAS 射击系统笔记\r\n## 第 2 篇：GA 技能架构与输入解耦 (Ability Architecture & Input Routing)\r\n\r\n### 一、 武器解耦哲学：从“大脑”退化为“U盘”\r\n在传统的初级 Demo 中，开火射线、弹药扣除通常写在 `AWeapon` 类中。在项目的 GAS 架构中，这是被严厉禁止的。\r\n*   **重构思想**：武器不再负责任何逻辑，它只是一个 **“数据载体”** 和 **“表现挂载点”**。\r\n*   **权限转移**：当玩家捡起（Equip）武器时，武器将自身携带的 `TSubclassOf<UGameplayAbility> FireAbilityClass` 交给玩家的 ASC。玩家 ASC 获得这个技能后，射击逻辑完全由玩家身上的 GA 来执行，武器只被动提供“枪口位置”和“特效资源”。\r\n*   **优势**：极大地降低了网络同步的复杂度（GAS自带同步），且实现了完美的多武器无缝切换（换武器 = 换 GA）。\r\n\r\n### 二、 GA 的实例化策略 (Instancing Policy)\r\n技能在内存中如何存活，直接决定了战斗系统的性能。我们在 C++ 基类的构造函数中将其锁死为 **`InstancedPerActor`**：\r\n1.  **`NonInstanced` (不实例化)**：全服共用一个 CDO（类默认对象）。性能极高，但**不要存储任何变量**，适用于极简的被动触发逻辑。\r\n2.  **`InstancedPerActor` (每个 Actor 一个实例)**：**项目枪械/连招的标准选择。** 玩家第一次获得技能时生成一个实例，此后反复调用。支持存储内部变量（如连发次数、是否正在换弹），且没有频繁创建销毁的开销。\r\n3.  **`InstancedPerExecution` (每次执行实例化)**：每次点击鼠标都 `NewObject` 一个新技能。极其安全（无脏数据残留），但高频射击时会导致严重的内存碎片和 GC 压力。\r\n\r\n### 三、 C++ 继承体系与“数据下沉”\r\n为了避免在蓝图里写出面条代码和无数个 `Cast to`（类型转换）节点，项目强制要求搭建严密的 C++ 继承链：\r\n*   **第一层：`UGameplayAbility`**（引擎原生，提供底层接口）。\r\n*   **第二层：`UBaseGameplayAbility`**（项目基类，绑定 `InputTag`，所有技能通用）。\r\n*   **第三层：`UWeaponAbilityBase`**（武器逻辑基类，封装 `GetSourceWeapon()`，提取武器的 DataAsset）。\r\n*   **第四层：`GA_Fire`**（蓝图纯逻辑层，只负责连线和时序控制）。\r\n*   **数据下沉法则**：如果一个变量（如 `WeaponDataAsset`）是核心配置，必须定义在 C++ 的基类中。否则蓝图在通过父类指针调用时，必须强转（Cast）到具体子类才能看见变量，严重破坏代码复用性。\r\n\r\n### 四、 执照发放：Spec 与 Handle 的区别\r\n赋予技能时，涉及三个极其容易混淆的核心概念：\r\n1.  **`UClass` (图纸/模板)**：即 `TSubclassOf<UGA>`，存在于硬盘上，告诉系统“这是什么技能”。\r\n2.  **`FGameplayAbilitySpec` (档案/说明书)**：结合了 UClass、技能等级（Level）、输入绑定（DynamicAbilityTags）以及 SourceObject（提供该技能的武器实例）。它是向系统申请技能的**“填好的表格”**。\r\n3.  **`FGameplayAbilitySpecHandle` (执照/钥匙)**：`GiveAbility` 执行后返回的唯一 ID。\r\n    *   *流转逻辑*：Character 将这把钥匙存在变量中。按下左键时，通过 `TryActivateAbility(Handle)` 启动引擎；丢弃武器时，通过 `ClearAbility(Handle)` 吊销执照。\r\n\r\n### 五、 全自动连发的状态机循环 (The Auto-Fire Loop)\r\n在 GA 蓝图中实现长按连发，必须构建严密的异步递归循环，不要使用普通的 `Delay` 节点。\r\n*   **启动与监听**：技能激活后，立刻开启 `Wait Input Release` 异步任务，并在 C++ 中通过 `Spec->InputPressed = true/false` 或 Tag 路由精确控制其状态。必须勾选 `Test Already Released` 以应对极快手速带来的竞态条件（卡壳 Bug）。\r\n*   **核心安全环 (Loop Circuit)**：\r\n    `自定义事件(ShootLoop)` -> **`Commit Ability` (安检扣费)** -> `分支(True)` -> `射线与表现` -> `Wait Delay (射速)` -> **回连 `ShootLoop`**。\r\n*   **为什么这是工业级？**：执行流必须每次绕回 `Commit Ability`，确保每一发子弹都在扣除后才射出。一旦 `Commit Ability` 返回 False（无弹药），立刻走向 `End Ability`，杜绝“白嫖子弹”和“死循环卡死”。\n","gas_shoot_3":"# UE5 GAS 射击系统笔记\r\n## 第 3 篇：表现层解耦与 Gameplay Cue 极致应用 (Presentation Decoupling & GC)\r\n\r\n### 一、 架构哲学：逻辑与表现的物理隔离\r\n在初级代码中，开发者习惯在开火函数里顺手写上一句 `PlaySoundAtLocation`。在项目的联机项目中，这是引发“网络风暴”的毒药。\r\n*   **项目原则**：服务器是瞎子和聋子，它只算数学题（GA、射线、伤害）。所有的音效、火花、弹孔、震屏，统统交给客户端的 **Gameplay Cue (GC)** 处理。\r\n*   **GC 的网络优势**：服务器只需要发送一个极其轻量级的 RPC，包含一个 `GameplayTag`（如 `GameplayCue.Weapon.Fire`）。客户端收到后，在本地自行解析并播放对应的资源。如果某个玩家在 1000 米外，系统甚至可以利用网络相关的距离剔除机制，直接不发送这个 GC 包，极限节省带宽。\r\n\r\n### 二、 Gameplay Cue 的两大派系与生命周期\r\n表现层分为两类，选择错误的父类会导致严重的性能泄漏：\r\n1.  **`GameplayCueNotify_Static` (静态瞬发型)**：\r\n    *   *适用*：枪口火花、开火音效、击中弹孔。\r\n    *   *机制*：它在内存中不作为独立的 Actor 生成（依托 CDO 执行）。**执行一次即焚**。重写 `OnExecute` 函数来处理逻辑。\r\n2.  **`GameplayCueNotify_Actor` (动态持续型)**：\r\n    *   *适用*：喷火器持续的火焰、激光光束、长时间的持枪护盾特效。\r\n    *   *机制*：是一个真实的 Actor，会被 `Spawn` 到世界中。拥有 `OnActive` (开始)、`OnRemove` (结束) 和 `Tick`，需要手动控制生命周期。\r\n\r\n### 三、 参数装箱与情报投递 (The Parameters Payload)\r\nGA 和 GC 是解耦的，GC 如何知道该在哪播放火花？全靠 `FGameplayCueParameters` 这个数据集装箱。\r\n在 GA 中使用 `Make GameplayCueParameters` 必须遵循项目装箱规范：\r\n*   **Instigator (煽动者)**：传入 `Get Avatar Actor`。告诉表现层是谁在开火。\r\n*   **Effect Causer (导致者)**：传入通过 Spec 拿到的 `BaseWeapon`（武器实例）。**这是重中之重！** GC 会根据这个武器去提取内部的 DataAsset（音效、粒子）以及获取枪口 Mesh 的位置。\r\n*   **Hit Result (物理报告)**：在处理击中表现时，必须将射线检测的 `Out Hit` 完整连入。\r\n\r\n### 四、 击中表现 (Impact) 的拆解与弹孔生成\r\n要实现“打铁冒火花，打木冒木屑”的 3A 级反馈，需要跨越三道防线：\r\n1.  **逻辑层探测开关**：在 GA 的 `LineTraceByChannel` 中，必须强制勾选 **`Return Physical Material`**。否则引擎为了省性能，传给 GC 的材质永远是空（None）。\r\n2.  **表现层洋葱剥解**：在 GC 的 `OnExecute` 中，绝对不能对 `Parameters` 连线使用 `Split Struct Pin`（分割结构体引脚，会破坏引用传递导致无法连回父类节点）。必须使用 **`Break GameplayCueParameters` -> `Break Hit Result`** 的正统拆解法。\r\n3.  **防崩判定与生成**：\r\n    *   提取出 `Phys Mat` 后，必须先接 `Is Valid` 防御性判定，再接 `Switch on EPhysicalSurface`。\r\n    *   **生成弹孔贴画 (Decal)**：使用 `Impact Point` 作为位置，将 `Impact Normal` 通过 **`Make Rot from X`** 转换为旋转体，确保贴画完美贴合墙面角度，并设置合理的 `Fade Screen Size`（如 0.005）优化远距离渲染性能。\r\n\r\n### 五、 动画的“政企分离” (Montage vs GC)\r\n动画也必须遵循解耦原则：\r\n*   **角色的身体动画（后坐力、换弹姿态）**：放在 GA 里，使用 `Play Montage and Wait`。这能通过 GAS 自动进行网络同步，并利用该任务的完成/打断回调来控制 GA 的状态结束。\r\n*   **武器的机械动画（滑套后退、抛壳）**：属于纯视觉，放在 GC 里。直接在 `OnExecute` 拿到 `Weapon Mesh` 并调用 `Play Animation`。\r\n*   **禁忌**：不要在角色 Montage 中使用 Anim Notify 去直接播放粒子特效，这会破坏武器的多样性（换枪不换动作就会错位），应统一交由 GC 驱动。\n","gas_shoot_4":"# UE5 GAS 射击系统笔记\r\n## 第 4 篇：属性核心运算、UI绑定与C++底层机制 (Attributes, UI Binding & C++ Under the Hood)\r\n\r\n### 一、 弹药体系与 Cost 机制 (The Cost System)\r\n项目不要在业务逻辑（如 GA 蓝图）中直接写 `Ammo = Ammo - 1`。所有核心数值的消耗必须交由 GAS 底层托管。\r\n*   **财务单据 (GE_Cost)**：创建一个 `Instant` 类型的 GE，使用 `Add` 和负数（如 `-1.0`）来定义消耗。\r\n*   **安检与扣费合一 (`Commit Ability`)**：\r\n    *   在 GA 内部，每次开火前必须调用 `Commit Ability`。\r\n    *   它会做两件事：**1. 预检 (Check)**：属性够不够扣？有没有处于冷却（Cooldown）？**2. 扣除 (Execute)**：如果条件满足，立刻应用 GE_Cost 扣除数值，并返回 `True`。\r\n    *   *优势*：将数值判定和扣除的原子操作封装在底层，彻底消灭“没子弹还能射击”的逻辑漏洞。\r\n\r\n### 二、 属性集的生命周期钩子 (Pre vs Post)\r\n`UAttributeSet` 中有两个必须死记硬背的核心虚函数，它们分管不同的职责：\r\n1.  **`PreAttributeChange` (守门员)**：\r\n    *   *时机*：任何修改属性的请求生效**前**调用。\r\n    *   *职责*：**纯粹的数值限幅（Clamping）**。通过 `FMath::Clamp(NewValue, 0.0f, MaxAmmo)` 确保子弹绝不会变成负数。不要在此处书写业务逻辑（如死亡、音效），因为系统在做预测计算时也会调用它。\r\n2.  **`PostGameplayEffectExecute` (事后判官/广播站)**：\r\n    *   *时机*：`Instant` 类型的 GE 成功修改基础属性**后**调用。\r\n    *   *职责*：**触发业务逻辑与 UI 更新**。\r\n    *   *案发现场 (`Data` 参数)*：通过 `Data.EffectSpec.GetContext().GetInstigator()` 获取凶手；通过 `Data.Target` 获取受害者。这里是执行“血量归零触发死亡事件”和“属性改变呼叫 UI 广播”的唯一正确地点。\r\n\r\n### 三、 C++ 委托全景图与 UI 动态绑定 (Delegates Architecture)\r\nUI 不应该通过 `Tick` 去轮询数据，必须使用事件驱动（Event-Driven）的委托机制。\r\n*   **四大委托家族**：\r\n    1.  **普通单播 (`DECLARE_DELEGATE`)**：C++ 内部点对点，速度极快，支持返回值，用 `ExecuteIfBound()` 调用。\r\n    2.  **普通多播 (`DECLARE_MULTICAST_DELEGATE`)**：C++ 内部广播，不支持蓝图。使用 `AddUObject()` 绑定成员函数（自带弱引用防崩溃保护）。\r\n    3.  **动态单播 (`DECLARE_DYNAMIC_DELEGATE`)**：支持蓝图反射，速度慢。\r\n    4.  **动态多播 (`DECLARE_DYNAMIC_MULTICAST_DELEGATE`)**：**UI 绑定的绝对主力**。支持在蓝图中 `Bind Event`。使用 `Broadcast()` 广播，使用 `AddDynamic` 在 C++ 中绑定。\r\n*   **项目红线 (反模式纠正)**：\r\n    绝不能用 `GameInstanceSubsystem` (全局单例) 去同步玩家个人的弹药和血量！在多人游戏中，这会导致所有人的 UI 跟着一个人跳动。必须将委托定义在**每个玩家私有的 `PlayerState`** 中。\r\n\r\n### 四、 换弹逻辑：属性间的原子转移 (SetByCaller)\r\n换弹（Reload）涉及弹匣（Ammo）和备弹（MaxAmmo/SpareAmmo）两个属性的联动。\r\n*   **逻辑闭环**：在 C++ 中精确计算 `AmountNeeded`（需要多少）和 `ActualTransfer`（实际能给多少）。\r\n*   **数据注入 (`SetByCaller`)**：\r\n    为了保证扣备弹和加弹匣是**同时发生（原子性）**的，我们不使用两次 `SetAttribute`，而是生成一张 `GE_Reload` 单据。\r\n    使用 `SpecHandle.Data.Get()->SetSetByCallerMagnitude(Tag, Value)`，将计算好的动态数值通过 Tag 注入到 GE 中，然后一次性 `Apply`。这保证了极佳的网络同步稳定性。\r\n\r\n### 五、 UHT 反射机制与内存传递契约\r\n在 C++ 与蓝图的交互中，函数的参数写法决定了引擎的编译与表现逻辑：\r\n1.  **`float Value` (传值)**：输入引脚。内存复制，安全但性能略低。\r\n2.  **`const float& Value` (常量引用)**：输入引脚。项目最常用的只读高性能传参。\r\n3.  **`float& OutValue` (非常量引用)**：被 UHT 翻译为**输出引脚 (Output Pin)**。用于回传处理后的结果。\r\n4.  **`UPARAM(ref) float& Value` (强制输入输出)**：如果你想原地修改一个变量（如传入一个大数组进行洗牌再输出），必须使用 `UPARAM(ref)` 宏，强迫蓝图保留左侧的输入引脚，避免深度拷贝造成的性能灾难。\r\n\r\n### 六、 数据下沉 (Data Lowering)\r\n项目追求“清爽的蓝图 (Clean BP)”。如果一个变量（如 `WeaponDataAsset`）是核心配置，**必须**在 C++ 的基类（`ABaseWeapon`）中声明。\r\n如果定义在蓝图子类中，任何对该变量的访问都会引发强制的类型转换（`Cast To BP_...`），导致代码耦合度极高。将变量下沉到 C++，并将函数返回值修改为精准的基类指针，可以彻底消灭无意义的 Cast 节点。\n","gas_melee_1":"# UE5 GAS 近战系统笔记\r\n## 👑 卷一：根运动控制与精准打击网 (Root Motion & Hitbox Mechanics)\r\n\r\n### 模块一：动画位移权限的“政企分离”（Root Motion）\r\n\r\n**【底层原理】**\r\n在动作游戏中，角色的位移分为两类：**主动位移**（推摇杆跑步）和**动作位移**（挥剑时的向前突进）。如果系统不把这两者的权限分开，就会导致“角色模型飞出碰撞胶囊体”或“原地空挥毫无打击感”的严重 Bug。\r\n\r\n**【项目标准处理流程】**\r\n1. **剥夺移动动画的位移权（Force Root Lock）**：\r\n   * *操作*：打开所有基础移动（Idle、Walk、Run）的动画序列（AnimSequence）。勾选 `Enable Root Motion` 和 `Force Root Lock`。\r\n   * *原理*：强行把这段动画的根骨骼（Root）钉死在原点 (0,0,0)。此时角色的移动 100% 由 `CharacterMovementComponent`（玩家输入）接管，手感最顺滑。\r\n2. **赋予攻击动画的位移权（Montages Only）**：\r\n   * *操作*：打开攻击蒙太奇，**只勾选** `Enable Root Motion`（绝不勾选 Force Root Lock）。同时在角色的动画蓝图（AnimBP）中，将 Root Motion Mode 设为 **`Root Motion from Montages Only`**。\r\n   * *原理*：平时移动靠键盘；一旦技能触发蒙太奇，引擎瞬间**接管胶囊体**，胶囊体会严格按照动画师 K 的根骨骼轨迹移动，实现极其精准的战斗步法。\r\n3. **避坑指南（假根运动）**：如果开启了上述设置，模型还是飞出去了，说明动画师做的是“假根运动”（位移做在了 Pelvis 胯部骨骼，Root 没动）。必须通过第三方插件或修改骨骼动画重新烘焙。\r\n\r\n---\r\n\r\n### 模块二：抛弃碰撞盒，建立“逐帧精准打击网”\r\n\r\n**【底层原理】**\r\n初学者喜欢在武器上绑一个 `Box Collision` 来检测伤害（Overlap）。\r\n**项目死刑警告**：碰撞盒的更新依赖物理 Tick。如果玩家挥刀极快（比如 0.1 秒挥完一刀），而在挥刀的中间几帧帧率波动，刀刃直接“穿过”了敌人，就会产生**漏判（穿模）**。此外，碰撞盒无法精确控制“只有刀锋划过才有伤害，收刀时碰到没伤害”。\r\n\r\n**【项目实现流程：AnimNotifyState (ANS) + Trace】**\r\n1. **空间定位（Socket）**：在武器网格体上添加 `TraceStart`（护手）和 `TraceEnd`（剑尖）两个插槽，定义剑刃的有效长度。\r\n2. **时间定位（ANS）**：创建继承自 `AnimNotifyState` 的蓝图（如 `ANS_MeleeHitbox`）。将其铺在蒙太奇中**刀刃真正发力的那几帧**，剔除前摇和后摇。\r\n3. **逐帧扫掠（NotifyTick）**：\r\n   * 在 `Received_NotifyTick` 中，每帧获取 Start 和 End 的当前世界坐标。\r\n   * 发射物理射线/形体检测。\r\n\r\n---\r\n\r\n### 模块三：物理检测的几何优化（解决“漏刀”空洞）\r\n\r\n**【底层原理：时序锯齿 (Temporal Aliasing)】**\r\n即使你逐帧检测，如果帧率是 60FPS，上一帧剑在左边，下一帧剑在右边，这两帧之间其实存在一个**巨大的扇形盲区**。\r\n\r\n**【项目进阶方案对比】**\r\n1. **`Sphere Trace` (单球体追踪)**：\r\n   * *机制*：发射一个球，打到第一个人就停止。\r\n   * *缺陷*：无法一刀同时砍中重叠的两个敌人。\r\n2. **`Multi Sphere Trace` (多球体追踪)**：\r\n   * *机制*：发射一个球，穿透一切，返回一个命中的数组（Array）。\r\n   * *缺陷*：如果半径小，依然会有帧间空缺（漏刀）。\r\n3. **`Multi Box Trace` (扁平盒体追踪) —— 工业级首选**：\r\n   * *机制*：将 Box 的尺寸设为扁长形（如 5x50x5），并**传入武器的当前 Rotation**。\r\n   * *优势*：宽大的刀刃形 Box 在扫过空间时，极大地弥补了帧与帧之间的间隙，形成一堵“密不透风的剑气墙”。\r\n4. **骨灰级方案（连点成面）**：在 C++ 记录上一帧的 `TraceEnd` 坐标，当前帧检测时，将上一帧的 End 和当前帧的 End 连起来做一个三角形扇面检测（《永劫无间》级做法）。\r\n\r\n---\r\n\r\n### 模块四：物理层级控制（拒绝 BlockAll）\r\n\r\n**【底层原理】**\r\n物理检测是极其昂贵的 CPU 开销。如果将 Character 设为 `BlockAll`，雷达会检测所有无关的频道（如摄像机、载具、视线），导致性能爆炸。\r\n\r\n**【项目物理分层流转图】**\r\n1. **定义新法则**：在 Project Settings 中新建 `Trace Channel`（如 `Weapon`）。默认响应设为 `Ignore`（所有人默认忽略刀剑）。\r\n2. **分配受击区**：只在玩家或敌人的 **Mesh（骨骼网格体）** 上的 Collision 设置中，将 `Weapon` 频道设为 `Block`。\r\n3. **精准打击**：`ANS_MeleeHitbox` 的 Trace 节点只发射 `Weapon` 频道的检测。\r\n4. **性能极致抠细节 (`TEnumAsByte`)**：\r\n   在 C++ 调用 `SphereOverlapActors` 寻找 `Pawn` 时，必须使用 `TEnumAsByte<EObjectTypeQuery>`。因为原生 C++ 枚举大小不固定（通常 4 字节），套上该模板强制压缩到 1 字节（8 位），完美对齐内存并兼容 UE 蓝图反射系统。\r\n\r\n---\r\n\r\n### 模块五：GAS 信号桥梁（物理与逻辑的剥离）\r\n\r\n**【底层原理：ANS 的单例陷阱】**\r\n**致命 Bug**：`AnimNotifyState` 是一个资源文件（CDO 单例）。如果两个 AI 同时挥刀，它们运行的是同一个 ANS 实例！如果你在 ANS 里存了一个 `AlreadyHitActors` 数组来防止重复扣血，AI 甲砍过玩家后，玩家进入数组，AI 乙再砍玩家就永远砍不中了！\r\n\r\n**【项目信号流转架构 (The Event Bridge)】**\r\n*   **ANS（观察者，不存状态）**：\r\n    1. `Trace` 发现命中 -> 提取 `Hit Actor`。\r\n    2. 创建包裹 `Make GameplayEventData`（Target = 被砍的人，Instigator = 挥刀的人）。\r\n    3. 调用 `Send Gameplay Event to Actor`（发给挥刀者自己），Tag 设为 `Event.Melee.Hit`。\r\n*   **GA_Melee（决策者，独占实例）**：\r\n    1. 技能启动时，并行挂起 `Wait Gameplay Event` 节点。\r\n    2. 收到信号后，GA 拿出**属于自己这个实例的** `HitActorsThisSwing` 数组。\r\n    3. 检查 Target 是否在数组中 -> 不在则 Add 并调用 `Apply Gameplay Effect`（真正扣血）。\r\n\r\n---\n","gas_melee_2":"# UE5 GAS 近战系统笔记\r\n## 👑 卷二：Combo 连招状态机与输入缓存 (Combo State Machine & Input Buffering)\r\n\r\n### 模块一：项目连招哲学 —— 摒弃“即时触发”，拥抱“输入缓存”\r\n**【底层原理】**\r\n*   **初级思维**：按下攻击键 -> `if (可以攻击) { 播放动画 }`。这种写法会导致玩家必须在动画**彻底结束的严格那一帧**按下按键才能连招。按早了没反应，按晚了连招断了，手感极其僵硬。\r\n*   **项目思维（Input Buffering）**：引入“记事本”机制。系统划定一个“连招窗口（Combo Window）”。如果玩家在这个窗口期内按了键，系统不立刻执行，而是**记录玩家的意图 (`bWantsNextCombo = true`)**。等当前动画自然播放到融合点时，系统查阅记事本，顺滑地切入下一招。\r\n\r\n### 模块二：状态的“红绿灯” —— 为什么不用布尔值？\r\n要划定“连招窗口”，我们需要在蒙太奇中放置 `AnimNotifyState`。\r\n**【项目死刑警告】**：不要在 ANS 中通过调用 GA 的函数来修改 `bool bCanCombo`！\r\n*   **致命缺陷**：如果在挥刀时角色被怪物击飞（动画强行 Interrupt），`NotifyEnd` 将永远不会触发，布尔值永远卡在 `True`。玩家复活后点一下鼠标，竟然离奇地触发了第二段连招。\r\n*   **工业级方案（Gameplay Tags）**：\r\n    *   在 `ANS_ComboWindow` 的 `NotifyBegin` 中：获取 Owner，`Add Gameplay Tag` (`State.Combo.WindowOpen`)。\r\n    *   在 `NotifyEnd` 中：`Remove Gameplay Tag` (`State.Combo.WindowOpen`)。\r\n    *   *原理*：Gameplay Tags 受 ASC 严格管理。如果技能中断或角色死亡，ASC 会自动清洗关联的状态标签，做到**绝对的内存与状态安全**。\r\n\r\n### 模块三：通信协议变更 —— 为什么近战不用 `Wait Input Release`？\r\n*   **射击逻辑**：机枪连发关注的是“持续状态”（按住不放）。所以用 `Wait Input Release` 监听手指抬起。\r\n*   **近战逻辑**：连招关注的是“瞬间触发（Trigger）”。玩家可能狂点鼠标。\r\n*   **C++ 改造方案**：\r\n    当玩家按下按键时，如果在 C++ 中发现当前 `GA_Melee` 已经处于 `Active` 状态：\r\n    调用 `ASC->HandleGameplayEvent(Tag, Payload)`，向 GA 发送一个如 `Event.Input.Attack` 的事件。\r\n    在 GA 蓝图中，使用 `Wait Gameplay Event` 并行挂起，专门用于接收这个“催更”信号。\r\n\r\n### 模块四：GA 蓝图里的“递归状态机” (The Recursive Loop)\r\n不要在蓝图里把第一段、第二段、第三段攻击拉成一条长长的“糖葫芦线”（面条代码）。必须使用**单点维护的递归循环**。\r\n1.  **双线并行 (Sequence)**：\r\n    *   `ActivateAbility` 后接 `Sequence`。\r\n    *   `Then 0`：启动 `Wait Gameplay Event` (监听按键)，如果此时身上有 `State.Combo.WindowOpen` 标签，则标记 `bWantsNextCombo = true`。\r\n    *   `Then 1`：调用自定义事件 **`PlayComboStep`**。\r\n2.  **`PlayComboStep` 核心流转**：\r\n    *   根据 `CurrentComboIndex` 从 `ComboMontages` 数组中提取当前动画。\r\n    *   执行 `Play Montage and Wait`。\r\n    *   **动画结束/混出时**：检查 `bWantsNextCombo`。\r\n        *   **若为 True**：`Index++` -> **强制重置 `bWantsNextCombo = false` (防止鬼畜连打)** -> 重新调用 `PlayComboStep` 自己。\r\n        *   **若为 False**：`End Ability`。\r\n\r\n### 模块五：消除“动画回弹 (Popping)”与 3A 级优化\r\n*   **现象**：如果在 `On Completed` 引脚衔接下一招，角色会有一瞬间闪回 Idle（待机）姿势。\r\n*   **修复方案 A（节点与资产配合）**：将循环线接在 `On Blend Out`（混出时），并将蒙太奇的 `Blend Out Time` 设为极短的 0.05s。\r\n*   **修复方案 B（项目终极方案）**：将三段连招整合进**同一个 Montage**，划分为 `Section1, Section2, Section3`。在 GA 中不重复调用播放，而是使用 **`Montage Jump to Section`**。因为执行流从未离开过当前蒙太奇，彻底消灭回弹。\r\n\r\n### 模块六：GAS 的“三权分立” —— 技能互斥与打断\r\n左键普攻，右键重击，如何防止玩家“左一下右一下”把动画卡抽风？\r\n利用 GA 配置中的三大标签容器：\r\n1.  **`Activation Owned Tags`**：激活后拥有。给普攻填入 `State.Attacking`。（技能开启时自动给角色贴上，结束时拔下）。\r\n2.  **`Activation Blocked Tags`**：屏蔽激活。给普攻和重击都填入 `State.Attacking`。\r\n    *   *效果*：只要有一个在攻击，另一个就绝对放不出来（互斥）。\r\n3.  **`Cancel Abilities with Tag`**：强行取消。如果要求“重击可以打断普攻”，在此处填入普攻的 Tag。重击启动瞬间，普攻 GA 会被底层直接 Kill 掉。\r\n\r\n---\n","gas_melee_3":"# UE5 GAS 近战系统笔记\r\n## 👑 卷三：打击感表现与空间数学奥义 (HitStop, Motion Warping & Spatial Math)\r\n\r\n### 模块一：打击感逻辑所有者 —— 顿帧 (HitStop) 与网络陷阱\r\n**【底层原理】**\r\n顿帧（HitStop / Hitlag）是动作游戏表现“刀刀入肉”的核心。当武器切中目标时，人为地让攻击者和受击者的动画产生极短时间的停滞（如 0.05 秒），给玩家大脑带来物理阻力的错觉。\r\n\r\n**【工程化实现方案与网络避坑】**\r\n1.  **错误的全局流 (`Global Time Dilation`)**：修改整个世界的时钟。在联机游戏中会导致所有玩家卡顿，绝对禁止使用。\r\n2.  **有缺陷的单体流 (`Custom Time Dilation`)**：修改特定 Actor 的时钟。\r\n    *   *网络灾难*：在多人网络下，服务器只同步胶囊体位置。如果客户端把 Actor 时钟调慢，客户端位置会落后于服务器。顿帧结束后，服务器会暴力把客户端**拉回（Rubber Banding）**，导致画面鬼畜瞬移。\r\n3.  **项目终极方案（视觉分离）**：**修改 `SkeletalMesh` 的 `GlobalAnimPlayRate`**（或蒙太奇的播放速率）。\r\n    *   *原理*：胶囊体的物理移动在服务器和客户端保持 1.0 的正常速度，**仅仅让骨骼动画停滞**。网络同步完全不受影响，特效（Niagara）也能保持正常速度飞溅，视觉体验最完美。\r\n\r\n### 模块二：顿帧去重机制 (HitStop Debounce)\r\n**【核心痛点】**\r\n大范围挥砍时，一刀可能同时扫中 5 个敌人。如果不做限制，触发 5 次连续的顿帧会导致画面完全卡死（卡顿时间疯狂叠加）。\r\n\r\n**【布尔锁（Boolean Lock）架构】**\r\n*   **设计思路**：每一“挥（Swing）”，无论砍中多少人，攻击者自身**只产生一次顿帧**。\r\n*   **实现流转**：\r\n    1. 在 `GA_Melee` 中设置局部变量 `bHasTriggeredHitStopThisSwing = false`。\r\n    2. 当接收到 `Wait Gameplay Event` 的命中信号时，检查该锁。\r\n    3. 若为 False，则执行顿帧，并立即设为 True。\r\n    4. 若为 True，跳过顿帧，但**依然执行扣血的 Gameplay Effect**。\r\n    5. 在下一段连招（`PlayComboStep`）开始前，将锁重置为 False。\r\n\r\n### 模块三：攻击吸附机制 (Motion Warping)\r\n**【底层原理】**\r\n为了解决“砍空气”的尴尬，现代 3A 动作游戏摒弃了代码硬写 `VInterpTo` 平移，全面采用基于动画的 **Motion Warping（运动扭曲）**。\r\n*   *机制*：引擎提取蒙太奇中原本的**根运动（Root Motion）位移曲线**，根据目标点的位置，对其进行动态的**拉伸或压缩**。动作快时滑步快，动作慢时滑步慢，绝对契合动画师的设计意图。\r\n\r\n**【配置全链路】**\r\n1.  **组件先行**：C++ 角色类必须挂载 `UMotionWarpingComponent`。\r\n2.  **动画标记**：在蒙太奇前摇阶段打上 `MotionWarping` NotifyState，配置**一字不差**的 `Warp Target Name`（如 `CombatTarget`）。\r\n3.  **状态清理**：吸附完成后或未找到目标时，必须调用 `RemoveWarpTarget`，防止角色在下一次攻击时被吸回旧的残余坐标。\r\n\r\n### 模块四：空间索敌雷达与向量数学 (Spatial Radar & Vector Math)\r\n项目的索敌绝不是简单的“找最近”，必须结合物理引擎与空间代数。\r\n\r\n**1. 物理探测 `SphereOverlapActors` 的底层限制**\r\n*   **参数严谨性**：使用 `EObjectTypeQuery`（如 Pawn），而非 `ECollisionChannel`（如 Visibility）。Overlap 的目的是快速大范围筛选身份，而非检测视线遮挡。\r\n*   **内存对齐魔法**：必须使用 `TEnumAsByte<EObjectTypeQuery>`。强制将 C++ 编译器分配的不定长 Enum 压缩为 **1 字节（8 位）**，优化内存并对接 UE 的反射蓝图虚拟机。\r\n\r\n**2. 核心数学武器：点乘与叉乘**\r\n*   **点积 (Dot Product)**：`|A| * |B| * cos(θ)`。返回一个**标量（数字）**。\r\n    *   *游戏应用*：利用两个归一化向量（`GetSafeNormal`）的点积来算夹角。判断目标是否在扇形视野内（如 `Dot > 0.5f` 代表在正前方 120° 内）。\r\n*   **叉积 (Cross Product)**：`|A| * |B| * sin(θ)`。返回一个**向量（三维方向）**。\r\n    *   *游戏应用*：遵循右手定则。判断敌人的攻击是从左边来还是右边来；计算斜坡地面的法线修正。\r\n\r\n---\n"};